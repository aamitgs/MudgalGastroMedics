# Preview deployment check

Throwaway file to give this branch a diff against `main` so GitHub allows
opening a PR, which triggers a Vercel preview build. Safe to delete once
the preview deployment registers.
