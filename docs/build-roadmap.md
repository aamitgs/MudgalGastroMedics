# MudgalGastromedics OS — Consolidated Build Roadmap

Derived from the six-part Master Prompt audit. Every item below was verified against
the running codebase, not assumed. This is a **build order**, not a feature wall:
work top-to-bottom, each track shippable on its own.

**Governing rule (from the blueprint):** every item here is backward-compatible and
pure UI + data-aggregation + performance. **None** touches authentication, RBAC, API
contracts, the database schema, or existing workflows. Auth/RBAC/audit/Postgres are
already done and are treated as frozen contracts.

**Legend:** Effort **S**≈hours · **M**≈1–3 days · **L**≈1–2 weeks · **XL**≈multi-week.
Priority reflects value-per-effort toward the "enterprise feel," not raw importance.

---

## Where things stand (one-line verdict)

The **spine is production-grade** — auth, RBAC (backend-enforced, zero gaps), audit,
the Postgres document backend, strict TypeScript (zero `any`), security headers,
adapter-based integration seams, and the test harness. The **enterprise experience and
scale layers** the blueprint describes are largely unbuilt. This roadmap builds those.

---

## TRACK 0 — Clinical Safety  *(highest priority)*
*Added after the Part 8 constitution, whose decision hierarchy ranks **Patient Safety
first, Visual Design last**. These are partial or missing today and outrank the
enterprise-UX tracks below. Every alert must be **explainable** (say why it fired) and
**non-blocking-by-default** for non-critical cases — warn, don't obstruct the clinician.*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 0.1 | ✅ **Active allergy alert at prescribe time** | Done. `AllergyGuard` (`components/DoctorPortalWorkspace.tsx`) shows a red, non-blocking alert per visit when the patient has a recorded allergy; acknowledgement is audit-logged (`clinical.allergy.acknowledged`). Closed the one gap found on 2026-07-07 audit: acknowledgement now captures an optional free-text reason (e.g. "switched to alternative drug"), sent to `app/api/clinical/allergy-acknowledged` and stored in audit metadata (`"Not specified"` when omitted) — matches "log the override with reason" without turning acknowledgement itself into a blocking gate | **Critical** | S | — |
| 0.2 | ✅ **Critical lab-result flagging** | Done — found already fully built on 2026-07-07 audit (roadmap was stale here). `lib/clinical/lab-critical.ts` auto-flags out-of-range results via threshold rules on save (`lib/lab-store.ts`); red badges in the lab queue and patient timeline; doctor notification via `lib/notification-rules.ts`; acknowledgement audited (`lab.critical.acknowledged`) | **Critical** | — | — |
| 0.3 | ✅ **Duplicate-patient prompt at registration** | Done. `app/api/patients/match` + the "Possible existing patient" banner (`components/AdminPatients.tsx`) already warned staff before saving. Closed the real gap found on 2026-07-07 audit: the backend used to **silently always merge** on phone match with no way to register a genuinely different person sharing a number (e.g. a family member) — a real data-integrity hazard (two people's clinical records could get conflated under one UHID). Added an explicit "Same person — update existing record" vs. "Different person — create a new record" choice; the latter sends `forceNew: true` (`lib/patient-store.ts`'s `createPatient`) and is audit-logged (`patient.created.duplicate_phone_confirmed`, linking both patient IDs) since it's a clinically significant deliberate override. Default behavior (merge) is unchanged | **High** | M | — |
| 0.4 | ✅ **Duplicate-medication detection** | Done — found already fully built on 2026-07-07 audit (roadmap was stale here). `lib/clinical/medication-overlap.ts` (`detectMedicationOverlap`) live-checks the new prescription against `currentMedicines` and shows an advisory amber alert in `components/DoctorPortalWorkspace.tsx`'s `PrescriptionField` | High | — | — |
| 0.5 | ✅ **Drug–drug interaction alerts** | Done (2026-07-08). `lib/clinical/drug-interactions.ts`: 11 curated, well-established high-risk pairs relevant to a general/GI hospital (warfarin×NSAIDs, warfarin×metronidazole, warfarin×macrolides, methotrexate×NSAIDs, statins×macrolides, domperidone×QT-prolonging drugs, tramadol×SSRIs, ACE-inhibitors×spironolactone, digoxin×loop-diuretics, sucralfate×fluoroquinolones, tramadol×ondansetron), each with an explainable mechanism + clinical guidance, not just "interaction detected." `detectDrugInteractions()` reuses `medication-overlap.ts`'s tokenizer (now exported as `drugTokens`) and checks three cases: new drug vs. current medicine, or two interacting drugs newly co-prescribed. Severity-tiered UI in `DoctorPortalWorkspace.tsx`'s `PrescriptionField`: `"high"` severity renders a new `InteractionGuard` (mirrors `AllergyGuard`, Track 0.1 — red alert, explicit "Acknowledge — reviewed" action, audit-logged per drug pair via a new `app/api/clinical/interaction-acknowledged` route, `clinical.interaction.acknowledged`); `"moderate"` severity stays a passive advisory (mirrors the Track 0.4 duplicate-medication check) so routine warnings don't dilute attention to the serious ones — alert fatigue is itself a safety risk. Never blocks; prescription autosave is unaffected either way. Fixed the sibling `allergy-acknowledged` route's audit call in passing: it was still spreading `auditRequestMetadata()` into flat `metadata` instead of the typed `device` field (the Track 1.6 pattern). 8 new unit tests; verified live end-to-end (high-risk alert renders → acknowledge → audit event has real `device` context; moderate match shows with no acknowledgement gate) | Start with a curated high-risk interaction list (explainable, sourced), alert at prescribe time; expand data source later. Never auto-block — warn + require acknowledgement | High | L | drug reference data |
| 0.6 | ✅ **Patient identity verification** | Done (2026-07-08). Confirmed genuinely unbuilt first (unlike several other "Not started" rows this session, an Explore pass found zero trace anywhere — no lib module, no UI, no matching commit). New `IdentityGuard` in `components/DoctorPortalWorkspace.tsx`, structurally mirroring `AllergyGuard` (Track 0.1), but a **gate, not a dismissible warning** — unlike the allergy/interaction alerts (legitimate clinical-judgment calls a doctor may override), there's no valid reason to write to a chart without first confirming it's the right patient, so the clinical note, prescription, advice and follow-up-date fields stay `disabled` until confirmed. Identifiers shown are name + phone (+ age when recorded) rather than name + phone/DOB per the original wording — `PatientRecord` has no discrete date-of-birth field, and adding one was out of scope for this item; age is the closest existing demographic field. Confirmation is a single click, audited per visit (`clinical.identity.confirmed` via a new `app/api/clinical/identity-confirmed` route). **Found and fixed a real state-leak bug during live verification**: `DoctorConsultationCard` (the parent housing the new gate) had no `key` at its call site, so switching patients re-rendered the same component instance instead of remounting it — confirming identity for Patient A would have silently carried over to Patient B without ever re-gating. Fixed by adding `key={selectedVisit.id}` at the call site (matching the existing pattern already used on `AllergyGuard`/`PrescriptionField` themselves). Verified live end-to-end: fields disabled pre-confirmation, enabled post-confirmation, audit event has real device context, and — the critical safety property — switching to a second patient correctly re-shows the gate rather than inheriting the first patient's confirmation | Lightweight confirm-identity step (name + phone/DOB) before clinical write actions on a record; audited | Med | M | — |
| 0.7 | ✅ **Consent capture & verification** | Done (2026-07-08). Confirmed accurate first (unlike several other rows this session, an Explore pass found consent genuinely was just a passive, unaudited checkbox for procedures and didn't exist at all for admissions). **Procedures**: `lib/procedure-store.ts`'s `updateProcedureSchedule` now rejects (`{ error }`) any status transition to `"Prep Started"`/`"In Procedure"`/`"Recovery"`/`"Completed"` unless `checklist.consent` is (or is becoming, in the same update) `true` — enforced server-side, not just in the UI. `components/AdminProcedures.tsx` pulls consent out of the generic 8-item checklist grid into its own amber/emerald-highlighted block explaining the gate. `app/api/procedures/schedule/route.ts` audits the specific false→true transition as `procedure.consent.recorded` (distinct from the other 7 unaudited housekeeping checkboxes). **Admissions**: `IpdAdmission` gained additive `consentRecorded`/`consentRecordedAt` fields; `ipdAdmissionCreateSchema` now requires `consentRecorded` (rejecting creation outright without it — there's no "pending" pre-admission status to gate later, since admission starts `"Admitted"` immediately) with a real UI checkbox in the admit-patient form; already covered by the existing `ipd.admission.created` audit event's `after` snapshot from Track 1.6, no separate audit call needed. **Two real bugs found and fixed during live verification**: (1) the exact same document-store in-place-mutation gotcha from Track 1.6 — the procedure route's "before" snapshot for the consent-audit comparison wasn't cloned, so it silently reflected the post-mutation state and the audit event never fired; fixed with `structuredClone`. (2) Testing this surfaced a real gap in the just-shipped Track 4.1 lazy-loading: navigating directly to a URL that already carries the module hash (a bookmark, a fresh `page.goto`, or the Patient Drawer's quick-action links) never mounted the target — my jump-nav fix only handled *clicks*. Fixed by also checking `location.hash` on `LazyModuleSection` mount | Real captured+audited consent step for procedures and admission (digital acknowledgement); block the workflow until consent recorded | Med | M | — |
| 0.8 | ✅ **Inventory batch/lot/expiry foundation** *(audit addendum)* | Found already fully built on 2026-07-08 audit — the roadmap was stale here (real commit `d7464ff`, "batch/lot/expiry foundation for stock traceability", predates this conversation). `lib/inventory-types.ts` has `batchNumber`/`lotNumber`/`expiryDate` plus an explainable `inventoryExpiryStatus()` (expired / expiring-soon within a 30-day window / not flagged); `lib/inventory-store.ts` persists all three on create/update; `app/api/inventory/route.ts` computes an `expiryAlerts` stat and supports an `expiryOnly` query filter; `components/AdminInventory.tsx` has entry-form inputs for all three, a combined Batch/Lot table column, an Expiry column with expired/expiring-soon badges, an "Expiry alerts" filter checkbox, and includes them in the CSV export. Verified live: created an expired item and an expiring-soon item, confirmed both badges render and the `expiryAlerts` stat counts them correctly | Add optional `batchNumber`/`lotNumber`/`expiryDate` to the inventory model + entry UI; surfaces expiring/expired stock so pharmacy expiry monitoring (P4) becomes buildable. Additive, backward-compatible | **High** | S | — |

**Track 0 outcome:** the platform actively prevents the highest-risk clinical mistakes —
allergy/interaction/duplicate-medication at prescribe time, critical results never missed,
no duplicate patient identities, consent always on file. This is the constitution's #1
priority and precedes cosmetic/enterprise-feel work.

> **Note on HDU vitals escalation** — already built (`computeHduEscalation`): flags
> overdue/out-of-threshold HDU vitals for staff attention. It is a staff-attention flag,
> never a diagnosis, and stays that way.

---

## TRACK 1 — Finish the Foundation
*Cheap, high-visibility, zero backend risk. Closes the most Part 1/2/6 gaps fastest.*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 1.1 | ✅ **Enterprise staff footer** | Done — confirmed on 2026-07-07 audit. `components/StaffFooter.tsx` (rendered by `StaffChrome.tsx`) has all 12 spec elements: OS name, tagline, category, live-`/api/health`-sourced Version/Environment/System Status, Last Sync, Privacy/Terms/Support links, Keyboard Shortcuts dialog, copyright | High | S | — |
| 1.2 | ✅ **Grouped sidebar IA** | Done — confirmed on 2026-07-07 audit. `HospitalOperatingSystem.tsx` renders `navGroupOrder.map(...)`, a genuine `role="group"` block per section with a visible header, not a flat list | High | M | — |
| 1.3 | ✅ **Adopt the shared `Button`** | DONE, closed out 2026-07-08 after a full sweep of every staff-facing file with a raw `<button>`. Migrated: `DoctorPortalWorkspace.tsx` (9 → 2), `AccessLogin.tsx` (6 → 2, all 4 `buttonClass` submit buttons across the credentials/password-change/MFA-setup/MFA-verify steps now share `ActionButton variant="primary"`; the now-dead `buttonClass` constant removed), `StaffChrome.tsx` (3 → 0: the header's search-palette trigger, dark-mode toggle and sign-out button were an exact hand-rolled duplicate — `border border-line ... text-muted ... hover:border-brand hover:text-brand` — repeated identically 3 times in this one file and again in `NotificationCenter.tsx`'s bell trigger; added a new **`outline` variant** to `ActionButton` carrying that exact class string rather than forcing an ill-fitting existing variant, a zero-visual-change de-duplication, not a redesign), and `DoctorLogin.tsx` (1 → 0: its submit button's gradient/shadow classes were a near-exact match for the existing `primary` variant). **Scope correction** (carried from earlier in this track): `ActionButton` is documented as the *staff*-facing button and has never touched a public page, so `PatientPortalAccess.tsx`, `PatientHealthDashboard.tsx`, `GalleryGrid.tsx`, `Header.tsx`, `AppointmentForm.tsx` and `LanguageToggle.tsx` (mounted only inside `Header.tsx`) are **out of scope** — migrating them would blur the "two products, never mixed" boundary. Every remaining raw `<button>` across the codebase (audited file-by-file: `WorkspaceLauncher.tsx`, `AdminPatients.tsx`, `HospitalOperatingSystem.tsx`, and the ~10 DataTable-converted `AdminX.tsx` modules — AiReviews/Appointments/Automation/BillingSummary/Communication/DoctorWorkflow/EnterpriseModules/Finance/HR/IpdBeds/Lab/OpdQueue/Pharmacy/Procedures — plus `GlobalCommandPalette.tsx` and `PatientClinicalSnapshot.tsx`) is a legitimate, deliberate exclusion, not remaining debt: the repeated bare "Open patient summary" name-cell link (same exact class string in ~10 modules, a text link with no border/background — no `ActionButton` "link" variant exists), `aria-pressed` card/pill selectors (template pickers, module pickers, common-test toggles, duplicate-patient confirm pair — active/inactive selection state doesn't fit ActionButton's plain-action shape), bare icon-only toggles with no border/background (password-visibility, command-palette favourite star), one invisible mobile-nav backdrop overlay, and `WorkspaceLauncher.tsx`'s public-site-token `bg-brand` "Continue" button (confirmed via grep to be shared only with `Footer.tsx`/`app/life-at-mgm/page.tsx`/`app/contact/page.tsx`, not a hand-rolled `ActionButton` duplicate) | Migrate remaining staff-facing raw buttons module-by-module; leave public-facing components on their own button styling | High | M | — |
| 1.4 | ✅ **Finish empty states** | Done (2026-07-08). The 18 Track-3.1-converted `AdminX.tsx` modules already got real empty states for free via `DataTable`'s `emptyState` prop. The 5 non-DataTable dashboards (`AdminReports`, `AdminAnalytics`, `AdminProductionReadiness`, `AdminEnterpriseModules`, `AdminSettings`) previously dropped ALL page content and showed only a bare red error paragraph when their initial fetch failed (a real gap vs. CLAUDE.md's "icon + why + how + actions" empty-state contract). Fixed in all 5: when there's no data at all and the load failed, render `ModuleEmptyState` (`AlertTriangle` icon, a specific title, `error` as the description, and a "Retry" action wired to the existing reload function) instead of a blank panel. When data *is* already loaded and only a background refresh fails, kept the existing lightweight inline banner above the stale-but-still-useful data, rather than replacing visible content with a full-panel error. `AdminEnterpriseModules` additionally needed its stats grid + module-list grid wrapped in `{modules.length > 0 ? … : null}` (it has no single "loaded" object to gate on, just `modules`/`records` arrays defaulting to `[]`). Verified live: normal load unaffected, simulated API failure shows the empty state + working Retry button, Retry recovers real content once the backend responds again | Bring the 5 remaining dashboards to a real empty/error state where meaningful | High | M | 1.3 |
| 1.5 | ✅ **Toast categories** | Done, exceeds spec — confirmed on 2026-07-07 audit. `lib/notify.ts` has 7 methods (success/info/warning/error/saved/loading/undo); called 59× across 18 files (up from 9) | Med | S | — |
| 1.6 | ✅ **Audit before/after + device** | Done (2026-07-08), scope corrected on investigation: the actual gap was worse than "missing before/after" — `app/api/opd/route.ts` (prescriptions, clinical notes, billing status/payment) and `app/api/ipd/route.ts` (bed status, admissions, transfers, escalation) called `recordAuditEvent` **zero times**, a real hole against CLAUDE.md's frozen "every mutation stays audited" contract. Fixed: `app/api/patients/route.ts` (POST now audits normal creates as `patient.created`, not just the duplicate-phone edge case; PATCH now audits `patient.updated` — previously unaudited entirely), `app/api/opd/route.ts` (POST → `opd.visit.created`; PATCH now splits into `opd.prescription.updated`/`opd.billing.updated`/`opd.visit.updated` by which fields changed, mirroring the existing field-level authorization split, severity `warning` for prescription changes), `app/api/ipd/route.ts` (admission create, bed status, transfer, escalation, and admission update/discharge all now audited; vitals deliberately left unaudited — high-frequency telemetry, not a record mutation, would drown the trail in noise). Also fixed a real correctness bug found in passing: `lib/ipd-store.ts`'s `transferBed` had its own `recordAuditEvent` call hardcoding `actorRole: "admin"` for every transfer regardless of who actually moved the patient, with no device context — moved that audit call up into the route handler where the real actor/device context lives. **Second bug found during live verification**: the document store caches records in memory and every `updateX()` mutates the found object in place, so a naive `before = await getXById(id)` taken right before calling `updateX()` ends up pointing at the *same* object `updateX` then mutates — before and after were identical by the time `recordAuditEvent` diffed them, silently producing empty change-sets. Fixed by wrapping every "before" snapshot in `structuredClone()` before the mutating call runs. Verified live end-to-end: created/updated a patient, prescription, billing status, admission and escalation, then read `/api/audit` directly and confirmed real field-level diffs (e.g. `billingStatus: "Not Started" → "Paid"`) and real `device` (`ip`/`userAgent`/`method`/`path`) on every event. **Codebase-wide sweep closed out 2026-07-09**: the same flat-metadata-spread bug (`metadata: { ...fields, ...auditRequestMetadata(request) }` or `metadata: auditRequestMetadata(request)`, instead of the typed `device:` field) had been found and fixed piecemeal in whichever route each earlier track happened to touch (1.12, 3.3, 4.3, 4.7 all mention fixing one instance in passing) but was never swept everywhere. Grepped every `recordAuditEvent` call site across `app/` and `lib/`: found 26 remaining offenders across 29 files (nearly every auth/session/patient-portal/CMS/lab/notification route that hadn't already been touched this session) — every one silently left `event.device` empty and mixed IP/user-agent/method/path flatly into business `metadata` instead. Fixed all of them to the correct `metadata: { businessFields }, device: auditRequestMetadata(request)` shape. Verified live: logged in via the legacy admin cookie, read `/api/audit` directly, confirmed a real event now shows `device: { ip, userAgent, method, path }` as its own top-level field, separate from business `metadata` (previously flattened together, `device` always `undefined`). Also ran `scripts/verify-auth-flows.mjs` (22/22) since `lib/access/guard.ts` was among the fixed files. **Found and fixed one more real, unrelated a11y bug while re-running the full e2e suite for this sweep**: `staff-a11y.spec.ts`'s "operations dashboard passes axe checks" was failing at `HEAD` (confirmed via `git stash` — reproduced identically before this session's changes, so pre-existing, not caused by the audit fix) — axe's `empty-table-header` rule flagged `AdminUserManagement.tsx`'s row-actions column header, which was a bare `header: ""` with no visible text (an `aria-label` on the button inside doesn't satisfy this rule; axe requires visible text for sighted low-vision/screen-magnification users, not just an accessible name). Grepped every `id: "actions"` column definition across every `AdminX.tsx` module adopted in Track 3.1: **17 of 19** had the identical `header: ""` gap (only `HospitalOperatingSystem.tsx`'s own patient-flow table already said `header: "Actions"`) — a systemic miss from the Track 3.1 DataTable-adoption sweep, not just one module. Fixed all 17 to `header: "Actions"`, matching the one instance that already had it right. Full verification chain green: `tsc`/`eslint` clean, 254/254 unit + 12/12 smoke, 19/19 e2e (the a11y test now passes), 22/22 auth-flow checks | Add `previousValue`/`newValue`/`device` to the audit event on create/edit/delete of Patients, Prescriptions, Billing, Beds (P4/P5) | Med | M | — |
| 1.7 | 🟡 **Feature-folder components** | IN PROGRESS (2026-07-08): moved all 23 admin-module components (`AdminReports` → `components/reports/`, `AdminPatients` → `components/patients/`, ... one folder per `module-*` id from `lib/access/admin-modules.ts`, e.g. `enterprise`, `doctor-workflow`, `ai-reviews`) — 49 → 26 flat files. `git mv` preserved history; each component had exactly one import reference (in `components/hospital-os/LazyModuleSection.tsx`'s dynamic-import map, a side benefit of Track 4.1's earlier refactor centralizing all 23 imports into one file) so this was genuinely low-risk — confirmed via `tsc` (zero relative imports inside any moved file, only `@/` absolute ones, so nothing else needed touching). Updated the smoke test's dynamic-import path check and 4 hardcoded file-path assertions that still pointed at the old flat locations. Verified live: all 23 modules mount and render real content from their new paths. **Deliberately left flat for a follow-up slice**: chrome/shell components with no single-domain home (`AdminModuleNav`, `AdminThemeShell` — the latter genuinely unused, found in passing, not removed since that's a separate concern from this move), auth components (`AccessLogin`, `AdminLogin`, `DoctorLogin` — arguably their own `components/auth/` domain, a reasonable next slice), and the doctor-workflow/monolith surfaces (`DoctorPortalWorkspace.tsx`, `HospitalOperatingSystem.tsx` — the latter explicitly the "one sanctioned monolith," not to be touched outside Track 4.10) | Move `AdminX.tsx` into `components/{patients,appointments,billing,pharmacy,laboratory,ipd,hr,…}` (P6). Pure move + import fixes | Med | M | — |
| 1.8 | ✅ **Reduced-motion + token cleanup** | Investigated on 2026-07-09 via Explore agent before building. Confirmed reduced-motion handling was ad hoc per-component (4 files each independently calling `useReducedMotion()`, no shared mechanism) and had a real gap: `HospitalOperatingSystem.tsx`'s `CommandPalette` component animated its result rows with no reduced-motion check at all. Also found `MotionReveal.tsx` used a 450ms transition — over CLAUDE.md's own 300ms cap. **Fixed both, systemically**: `components/AppChrome.tsx` (the single root switch every route renders through) now wraps every branch in `<MotionConfig reducedMotion="user">` — every `motion.*` component app-wide, present and future, automatically honors the OS-level reduced-motion setting without needing its own `useReducedMotion()` call; this closed the `CommandPalette` gap without touching `HospitalOperatingSystem.tsx` (the sanctioned monolith) at all. `MotionReveal.tsx`'s transition cut to 300ms. Verified live: `reducedMotion: "reduce"` context emulation across the public site, Hospital OS, and the Command Palette all load/open correctly. **Token cleanup**: found the identical brand-gradient hex literal (`linear-gradient(135deg,#0ea5c2,#087d9e)`) copy-pasted across 5 files (`ButtonLink.tsx`, `AppointmentForm.tsx`, `GalleryGrid.tsx`, `PatientPortalAccess.tsx`, `design-system/ActionButton.tsx`) — added a single `--site-brand-gradient` token in `globals.css` and repointed all 5; verified live the computed `background-image` resolves to the exact original color values (zero visual change, pure de-dup). **Evaluated and deliberately left alone**: `Header.tsx`'s `#19c7f3` nav-hover color and `Footer.tsx`'s `#111111`/`#969696` — both single-file, single-occurrence values with no exact match to any existing token (footer's near-black neutral is plausibly an intentional departure from the site's teal-tinted palette, not accidental drift); converting either would risk a visible, unrequested color-scheme change on a guess, unlike the 5-file gradient duplication which was an unambiguous, safe win. **Deliberately not attempted**: the broader `--hos-*` vs. site-token class-vocabulary convergence (~500 references across 13 files, mostly `HospitalOperatingSystem.tsx`) — git history shows the color *values* were already unified (`85ceb4b`, Track 4.9), but the class-name split is structurally tied to the monolith and correctly belongs with Track 4.10's decomposition, not a broad sweep here; also left the remaining ~140 hardcoded hex/shadow/radius occurrences across the app untouched — most are legitimate one-off chart/PDF colors, and a full sweep would be a large, low-priority, low-confidence undertaking disproportionate to this item's Low priority | Extend `prefers-reduced-motion` to admin; move stray hex to tokens (keep intentional CTA gradients documented) | Low | S | — |

**Track 1 outcome:** staff surfaces look and behave like one enterprise product;
navigation is grouped; one Button everywhere; every table/list has a real empty state.

### Track 1 addenda — July 2026 full-prompt audit
*Verified against the codebase on 2026-07-06. Items 1.3/1.4/1.5 shipped their
primitives but not full adoption; the audit also surfaced small spec gaps and two
cheap Part 1/3 violations. All additive, zero backend risk.*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 1.9 | ✅ **De-mock the OS dashboard metrics/trend/feed** | Done (2026-07-08), scope corrected on investigation: `dashboardMetrics`/`analyticsSeries` turned out to be dead fallback code already — `/api/hospital-os/snapshot` always computes real metrics/trend live via `createAnalyticsSnapshot()`, never omits them. The actual bug was two *initial-state placeholders* shown before the first fetch resolved: the `useQuery` default `data` (fake "42 OPD / 76% beds / Rs 4.8L") and `realtimeMessages`' initial state (fake specific events like "Nurse Priya S..."). Fixed: `HospitalOperatingSystem.tsx`'s `DashboardOverview` now takes `isLoading` and renders `<Skeleton>` placeholders (reusing the existing shadcn primitive, not fabricated numbers) for the metric tiles and both charts; the realtime feed shows a neutral "No recent activity yet" / "Connecting to live activity…" message when empty instead of canned events. The three now-fully-dead constants were deleted from `lib/hospital-os-data.ts`. Also fixed in passing (same block, pre-existing bug this session repeatedly saw in e2e console output): `realtimeMessages` used the message text itself as the React list key, causing "duplicate key" warnings whenever the same event text recurred — now a generated `{id, text}` pair. **Follow-up also fixed (2026-07-08)**: `PatientWorkspace`'s summary tab — both the `ClinicalBrief` card (hardcoded "Age/Sex 42/Male, Abdominal pain, reflux, Risk Moderate, Star Health preauth pending" for *every* patient, worse than the metrics bug since `age`/`risk`/`insurance` were already real fields sitting unused on the same `activePatient` prop) and the "Vitals"/"Prescription"/"Next action" cards (same "BP 126/82..." text for every patient) — were the more clinically sensitive of the two Track 1.9 issues, since they looked like real per-patient data rather than a dashboard-wide number. Fixed by extracting `components/hospital-os/PatientClinicalSnapshot.tsx` (same extraction pattern as `PatientTimelinePanel`, toward 4.10), reusing `/api/patients/summary` (already built for the Global Patient Drawer, Track 2.1 — zero backend changes needed): Age/Sex/Risk/Insurance now read from real `activePatient`/patient-record fields; "Primary Concern" from the visit's recorded `symptoms`; "Clinical Note" and "Prescription" cards (renamed from "Vitals" — no discrete vitals data source exists for OPD-context patients, and mislabeling free-text `clinicalNote` as structured vitals would itself be misleading) from the visit's real `clinicalNote`/`prescription` fields; "Next action" synthesized from real signals in priority order (unacknowledged critical labs → follow-up date → outstanding balance → next appointment → "No pending action flagged"). Verified two different seeded patients render provably different content end-to-end | **High** | S–M | — |
| 1.10 | ✅ **Primitive completions** | Done — found already fully built on 2026-07-07 audit (roadmap was stale here). `ActionButton` has a `"warning"` variant; `notify.ts` has `loading`/`undo`; `ModuleEmptyState` has `secondaryAction`/`helpHref` props | High | S | — |
| 1.11 | ✅ **Retire HMS/ERP positioning language** | Done (2026-07-08). Moved `app/hms-erp/page.tsx` → `app/operations/page.tsx` (new canonical route, matching the page's own existing "Operations dashboard" framing) with a permanent redirect `/hms-erp` → `/operations` in `next.config.mjs` for SEO continuity. Reworded every "HMS"/"ERP"/"Hospital ERP" mention across `app/operations/page.tsx`, `app/platform/page.tsx`, `app/ai-planning/page.tsx`, `app/admin/page.tsx`'s metadata, and `lib/platform-data.ts` (renamed `hmsFeatures` → `operationsFeatures`) to "Enterprise Healthcare Platform"/"Operations Platform"/"hospital operations" language. Deliberately left internal-only code untouched (`lib/hms-modules.ts`, `app/api/hms/route.ts`, `lib/rbac.ts` comments) — those are technical identifiers for the authenticated "Enterprise Modules" admin screen, never shown to the public, and renaming them would be unrelated internal churn with no positioning benefit | Med | S | — |
| 1.12 | ✅ **Zod convergence on REST routes** | Started 2026-07-08, closed out 2026-07-12. Six slices done — **Patients, OPD, IPD, HR, Procedures schedule, HMS** — all onto per-route files in `lib/validation/` plus a shared `lib/validation/http.ts` (`firstZodIssueMessage`). Key lessons carried forward: (1) **always live-verify against the real calling component**, not hand-picked payloads — caught a real `null`-vs-`undefined` bug this way (Patients slice) that hand-picked payloads never would have; (2) where a route inspects the body *before* `authorize()` to pick which permission to check, parse first and read the resource from the typed result, preserving the exact existing order (OPD, IPD); (3) preserve intentionally-lenient existing fallbacks (invalid enum silently defaults rather than rejects) as `.default(...)` where the real caller can't trigger the distinction anyway (HR, HMS). **HMS** (`app/api/hms/route.ts` + `lib/validation/hms.ts`) also fixed two pre-existing bugs found in passing: both POST/PATCH hardcoded `actorRole: "admin"` on every audit event regardless of who actually acted (now `auth.context.activeRole`/`userId`), and both used the old flat-`metadata`-spread pattern instead of the typed `device` field. Verified live: rejection on missing fields, a real record created/updated through the actual `AdminEnterpriseModules` UI, and both audit events now carry the correct actor role and real device context. **Follow-up noted from the IPD slice, still not fixed**: `lib/audit-diff.ts`'s default redact list treats any field named `token` as a secret, incorrectly redacting `IpdAdmission.token` (a public queue number, not a secret) — cosmetic only. **Auth-critical batch closed out 2026-07-12** (re-audited: several routes built since — external-referrals, purchase-orders, staff-notes, feedback, announcements — were already Zod-native from the start, so real remaining count was 34 write-handling routes, not 56): `lib/validation/auth.ts` now covers all 9 highest-risk routes — `auth/{login,password,role,break-glass}`, `auth/mfa/{setup,verify}`, `admin/session`, `doctor/session`, `access/users`, `access/approvals`. Same lessons applied: `auth/login` preserves the single generic "Invalid username or password" 401 for any parse failure (not per-field messages, to avoid leaking which credential was wrong); `access/users` PATCH keeps `id`/`operation` as lenient `.default("")` rather than `.min(1)`, since an empty id already falls through to the existing 404 "User not found" and an unrecognized operation already falls through to "Unknown operation." — forcing 400s there would have been a real behavior change, not just validation. Verified live end-to-end: full `scripts/verify-auth-flows.mjs` (22/22), plus targeted checks the standard script doesn't cover — MFA setup/verify, break-glass (both the too-short-reason 400 and a valid grant) with a real newly-created main-doctor RBAC session, legacy admin/doctor passcode logins, and role-switch-to-unheld-role 403, all via real sessions, not mocked. 254/254 unit + 12/12 smoke green. **Patient-auth batch closed out same day**: `lib/validation/patient-auth.ts` covers `patient/auth/{login,profile}`, `patient/auth/magic-link/{request,verify}`, `patient/auth/otp/{request,verify}`, `patient/family`, `patient/appointments` — 8 more routes. **One real regression caught and fixed during live verification, not assumed correct from reading the diff**: `familyMemberCreateSchema` initially had `name: z.string().trim().min(1, "...")`, which looks equivalent to the store's own `!name` check but isn't — omitting the field entirely (not just sending `""`) fails Zod's *type* check before the `.min()` message ever applies, surfacing a raw `"Invalid input: expected string, received undefined"` instead of the friendly message. Fixed by leaving `name` a lenient `.default("")` and keeping `addFamilyMember`'s own `!name` check as the single source of truth for that message, exactly like the `id`/`operation` lenience elsewhere in this track. Also fixed a leftover flat-metadata-audit-spread bug found in passing in `patient/auth/magic-link/verify` (`metadata: meta` → `device: meta`). Verified live: weak-password rejection, magic-link anti-enumeration + malformed-email + bad/missing-token paths, family add/remove, and email+password login all exercised with real sessions; full auth-flow script re-run (22/22), 254/254 unit + 12/12 smoke green. **Clinical + AI batch closed out same day**: `lib/validation/clinical.ts` covers `clinical/{allergy,identity,interaction}-acknowledged` and `ai/{discharge-summary,patient-summary,reviews}` — 6 more routes. **Applied the patient/family lesson proactively this time**: every "required" field in this batch uses a lenient `.default("")` with the exact original combined-message truthy check kept in the route, rather than `.min(1)` on the schema — avoiding the raw-Zod-error regression before it could happen, not after. **Two more `actorRole: "admin"` hardcoding bugs found and fixed in passing** in `ai/reviews` (POST's seed/generate branches and PATCH), the same class of bug fixed in HMS/Automation earlier — now `auth.context.activeRole`/`userId` throughout. Verified live with a real newly-created main-doctor session: all three clinical-acknowledgement routes and both AI-summary routes reject with their original friendly combined messages and succeed with valid input; `ai/reviews` seed/generate/update exercised via the legacy admin session (needs `cms`), and the audit trail confirmed `ai.reviews.seeded`'s `actorRole` now reads `super-admin` instead of the previously-hardcoded `admin`. 254/254 unit + 12/12 smoke green. **Final operational batch closed out the same day — all 62 route files now converged**: `lib/validation/operations.ts` covers `appointment` (both the public booking POST and the status PATCH), `automation`, `cms`, `communication`, `finance`, `inventory`, `lab` (PATCH only — its POST has zero route-level validation to converge, pure passthrough to `createLabOrder`, left untouched rather than wrapping it in a schema that would validate nothing), `notifications`, and `mobile/v1/patient`. `pharmacy`'s POST is the one other deliberate non-conversion for the same reason. Every store-delegated create (`createAutomationTask`, `upsertCmsContent`, `createCommunicationLog`, `createInsuranceClaim`/`createAccountEntry`, `upsertInventoryItem`) uses `z.looseObject(...)` validating only the couple of fields the ROUTE itself inspects (mode/action/type/status discriminators), letting every other field pass through untouched to the store's own `normalizeText`-based validation — nothing gets silently stripped. **Found and fixed a real, high-severity bug during live verification, not assumed correct from the diff**: the public appointment-booking schema's required-field coercion (`z.unknown().transform(...)`, no `.optional()`) rejected an entirely-absent key at the *type* level before the transform ever ran — meaning submitting the public form with even one of name/phone/service missing failed the **whole parse**, and the route's fallback-on-failure path substituted an all-empty object, silently discarding every field the visitor *did* fill in. Caught by testing `{ name: "X" }` alone and seeing `"Missing: name, phone, service"` — all three, not just the two actually missing. Fixed by adding `.optional()` before the transform. Re-verified: a name-only submission now correctly reports only `phone, service` as missing, and a full realistic booking (all ~15 fields) round-trips with every field intact; also stress-tested malformed JSON and a bare-array JSON body against this unauthenticated public endpoint — both degrade to the same friendly 400, never a 500. Live-verified every other route in this batch with a real admin session: automation generate/create/update, CMS create+publish, communication log create, finance account-entry create, inventory create+adjust, lab PATCH, notifications read-all, and the mobile API's token-required 401. 254/254 unit + 12/12 smoke green throughout. **Track 1.12 is now complete**: all 62 original route files plus every route added later in the session are on one shared Zod validation culture | Migrate route bodies onto shared Zod schemas module-by-module (P5 "validate everything", one validation culture) — DONE | Med | M | — |

**Adoption completion (amend 1.3/1.4/1.5), re-measured 2026-07-07:** 63 raw `<button>`s
remain across 27 files (`ActionButton` itself is healthy — 25 files, 83 sites); 18/23
DataTable-converted modules get real empty states via `DataTable`'s `emptyState` prop,
5 non-list dashboards don't; `notify` used in 18 files (up from 9). Finish 1.3's rollout
module-by-module alongside other Track 1/2 work.

---

## TRACK 2 — Structural UX
*The "enterprise feel." Medium effort; each item independently valuable.*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 2.1 | ✅ **Global Patient Drawer** | Found already fully built on 2026-07-08 audit — the roadmap was stale here (real commit `5cf0ae0`, "Global Patient Drawer — one-click patient context (Track 2.1)", predates this conversation). `components/hospital-os/PatientDrawer.tsx`, mounted once in `StaffChrome.tsx` (available on every staff page) and opened by `usePatientDrawerStore` from **14 different component files** — essentially every module with a patient name column. Reads `/api/patients/summary` and shows: UHID, age/sex/blood group/status, allergies (always rendered, explicit "No allergies recorded" when none — safety info first, per the doc comment), unacknowledged-critical-labs count, chronic conditions, current medication, active admission (ward/bed/diagnosis/doctor), next appointment, outstanding balance broken down by source, insurance-use history, recent lab orders (with a Critical badge), a recent-activity timeline, and 6 quick-action links (book appointment, OPD queue, order lab, billing, admit/IPD, pharmacy). Verified live: opened from the Patients module with a seeded patient, confirmed allergies/meds/quick-actions render with real data (not placeholders). **Minor, non-blocking gap**: no patient photo (no photo field exists anywhere in the data model, so not applicable yet) and no direct "print Rx"/"discharge" quick-action links (discharge is reachable via the Admit/IPD link; print Rx isn't linked at all) — small polish, not worth blocking on | Side drawer on any patient click: photo, UHID/MRN, age/sex, blood group, allergies, alerts, meds, diagnosis, doctor, recent visits, next appt, outstanding bills, insurance, recent reports + quick actions (book, print Rx, invoice, lab, admit, discharge, open timeline). One shared component used app-wide | **High** | L | patient-summary read endpoint (small) |
| 2.2 | ✅ **Role-filtered modules** | Found already fully built on 2026-07-08 audit — the roadmap was stale here (real commit `b23c11a`, "role-filtered operations modules (Track 2.2)", predates this conversation). `lib/access/admin-modules.ts`'s `visibleAdminModules(role)` filters the 23-module registry against the same `roleHasPermission` matrix the server enforces, computed server-side in `app/admin/page.tsx` (no client `/api/auth/me` fetch needed — even better than the original build note, since it avoids a flash of unfiltered content). `AdminModuleNav`'s jump-nav receives the *same already-filtered* `modules` array, so it structurally can't drift out of sync with the rendered sections. **Caught my own test mistake during verification**: testing role-filtering via the legacy `/api/admin/session` login always resolves to `activeRole: "super-admin"` regardless of which staff username logs in through it (documented, intentional — "the legacy admin login has always been the full-access operator account") — that endpoint is the wrong tool for testing per-role restrictions. Verified properly via a real RBAC login (`/api/access/users` → `/api/auth/login` with role `reception`): saw exactly the 10 modules the permission matrix predicts (automation/ai-reviews/patients/appointments/opd/procedures/ipd/billing/finance/communication), not all 23 | Fetch `/api/auth/me` once; render only modules the active role may view; jump-nav filters identically (P1/P2/P3). Server already enforces — this aligns UI to it | **High** | M | — |
| 2.3 | ✅ **Per-role dashboards** | Found already fully built on 2026-07-08 audit — the roadmap was stale here (commit `0807f0a`, "per-role Today command band on /admin (Track 2.3)", predates this conversation; has its own unit test file). `lib/role-dashboard.ts`'s `buildRoleDashboard()` exhaustively switches over every `AccessRole` (super-admin/admin → "Hospital today", main-doctor/duty-doctor → "Clinical today", nurse → "Ward today", reception/pro → "Front desk today", lab-technician → "Laboratory today", pharmacist → "Pharmacy today", billing-accounts → "Finance today", hr → "People today", patient → hidden) — covers every named role from the roadmap (Doctor/Reception/Nursing/Lab/Pharmacy/Billing/Admin; "Management" maps to the same admin/super-admin band, since there's no separate Management `AccessRole`). Each returns live tiles (color-toned by attention/critical state) + quick-action links, rendered server-side by `components/hospital-os/RoleTodayBand.tsx` at the top of `/admin` from the same analytics snapshot the OS dashboard uses — same design language, role-specific content. Verified live: super-admin sees "Hospital today," a real reception RBAC login sees "Front desk today" with a working "Register patient" quick action | Role landing dashboards (Doctor/Reception/Nursing/Lab/Pharmacy/Billing/Admin/Management) — same design language, role-specific widgets + quick actions + empty states (P3) | High | L | 2.2 |
| 2.4 | ✅ **Notification inbox** | Found almost fully built on 2026-07-08 audit — the roadmap was stale here. `components/hospital-os/NotificationCenter.tsx` (explicitly labeled Track 2.4): bell with unread/critical-aware badge, popover tray, full `Unread/Read/Resolved/Archived` lifecycle, `Open/Unread/Critical/Archived` filters, category grouping (`notificationCategories`, role-permission-gated per category via the same matrix the server enforces), a real empty state, 60s polling. `lib/notification-rules.ts`'s `evaluateNotificationRules()` (explainable — every notification's `detail` says why it fired) already covered low-stock, expiry, urgent-appointment, HDU escalation, critical-lab, escalated-AI-review and long-OPD-wait — 7 of the 8 named/implied sources. **One genuine gap closed today**: "overdue turnover" (the roadmap's own named example) wasn't implemented, even though the underlying stat (`bedsOverdueTurnover` in `getOccupancyStats()`) already existed. Added a `turnover:` rule (beds stuck in `Cleaning` past `turnoverOverdueMinutes`, moved from `ipd-store.ts` into `ipd-types.ts` alongside `hduVitalsOverdueMinutes` for consistency) firing an Administrative-category notification. 2 new unit tests (11 total in the suite); verified live end-to-end: set a real bed to Cleaning via the API, backdated its `statusUpdatedAt` in the JSON store, restarted the dev server (in-memory cache only reads from disk on process start — the same caching behavior found during Track 1.6), and confirmed the notification appears both via `/api/notifications` and in the real bell tray | Bell + tray in StaffChrome/OS: unread, priority, critical, assigned, resolved, archived, grouped by department; sourced from the audit stream + alert rules (low stock, HDU escalation, overdue turnover) (P3/P4) | High | L | — |
| 2.5 | ✅ **Command palette upgrades** | Found already fully built on 2026-07-08 audit — the roadmap was stale on all three of 2.5/2.6/2.7 (single commit `96f640f`, "global command palette on live data (Tracks 2.5/2.6/2.7)", predates this conversation, closes all three at once). `components/hospital-os/GlobalCommandPalette.tsx`: Ctrl/Cmd+K shortcut, quick-create actions matching the roadmap's own named examples exactly ("New patient," "Book appointment," "Generate invoice," plus "Order lab test"), a favourite-star toggle on every quick-create item, a "Recently viewed" patients section, mounted once in `StaffChrome` (available on `/admin`, `/doctor` and `/login`). Verified live: Cmd+K opens it, quick-create actions render before typing, favouriting persists, live search finds a just-created real patient, selecting it opens the Patient Drawer and records it as recent | Add recent records, favourites, quick-create actions ("New patient", "Book appointment", "Generate invoice"); mount on `/admin` and `/doctor` (P3/P6) | Med | M | — |
| 2.6 | ✅ **Global search on live data** | Found already fully built alongside 2.5/2.7 (same commit `96f640f`). `lib/global-search.ts`: real ranked search (exact/prefix/substring scoring) over `PatientRecord`/`AppointmentRecord`/`LabOrder`/`InventoryItem`/`IpdAdmission`/`StaffMember` — pure and unit-tested (`tests/unit/global-search.test.ts`, 13 tests, all passing) so the ranking logic doesn't need the document store to verify. `app/api/search/route.ts` queries the live stores directly and gates each category behind the same `roleHasPermission` matrix the server enforces everywhere else — a role never sees a search category it couldn't otherwise open. Verified live against a freshly-created patient, not a fixture | Back the palette with a `/api/search` over real stores (SQL `ilike` in DB mode); categorized results (P3) | Med | M | 2.5 |
| 2.7 | ✅ **Recent activity + Favourites** | Found already fully built alongside 2.5/2.6 (same commit `96f640f`). `stores/command-history-store.ts`: a `zustand` store with `persist` middleware (localStorage-backed, survives reloads) tracking up to 8 recently-viewed patients and 12 favourited commands — scoped to patients + commands rather than the roadmap's broader "reports/invoices" wording, a reasonable, still-substantial interpretation given those are the two record types the palette actually surfaces. Verified live: favouriting a quick-create action and viewing a patient via search both persist correctly and reappear on reopening the palette (confirmed via a direct `localStorage` read, not just UI inference) | Recently-viewed patients/reports/invoices; favourite patients/reports/commands (P3) | Med | M | 2.1, 2.5 |

**Track 2 outcome:** each role enters its own workspace; the patient drawer makes
context one click away; a real notification inbox and live global search.

---

## TRACK 3 — Enterprise Data
*Scale-critical. Larger, but unlocks "not a CRUD app."*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 3.1 | ✅ **Shared enterprise DataTable** | ✅ Primitive built. ✅ **Patients**, ✅ **Appointments**, ✅ **Laboratory**, ✅ **OPD Queue**, ✅ **Pharmacy**, ✅ **IPD & Beds**, ✅ **Inventory**, ✅ **HR**, ✅ **Finance**, ✅ **Procedures**, ✅ **Communication**, ✅ **CMS**, ✅ **Doctor Workflow**, ✅ **Billing Summary**, ✅ **AI Reviews**, ✅ **Audit Log**, ✅ **Automation**, ✅ **User Management/Access** adopted (`lib/*-query.ts` per module) — **18 of 23 admin modules**, every one that is genuinely a searchable/sortable list of records. User Management: client-paginated (small realistic staff-account volume, same reasoning as HR), consolidates the 4-5 per-row security actions (reset password, reset MFA, suspend/reactivate, role-change request) behind one "Manage" detail panel with every `disabled={!isSuperAdmin}` condition (plus the finer-grained ones — can't suspend self, can't reset MFA never set up) preserved verbatim; the one-time-credential and two-person-rule approval banners stay untouched as persistent inline banners, not toasts, since they carry instructions an admin needs time to read/act on. Verified live with a non-super-admin ("admin" role, MFA completed via `otplib` in the test script) session: "Viewing only" banner shows, every mutation button renders disabled (never hidden) — backend `authorize()`/RBAC is unchanged regardless. **Remaining 5 evaluated and correctly not converted** (dashboards/fixed reference data, not lists): Reports, Analytics, Enterprise Modules, Production Readiness, Settings — see prior audit notes. Track 3.1 is complete | Track 3.1 complete. Next: 3.2 (advanced forms) or another track | **High** | L | — |
| 3.2 | 🟡 **Advanced forms** | Primitive built and piloted on 2026-07-09; confirmed via Explore agent beforehand that no shared RHF+Zod wrapper existed — 3 ad hoc RHF forms in `HospitalOperatingSystem.tsx` each duplicating their own `applyIssues`/`setError` submit-time validation, 14+ others on plain `FormData`, `@hookform/resolvers` not installed. Built `hooks/useAdvancedForm.ts`: a ~15-line hand-written Zod-to-RHF `Resolver` (reusing the same issue-to-field-map shape as the monolith's `applyIssues`, but wired into RHF's real `resolver` option + `mode: "onBlur"` for true inline validation instead of submit-only) — zero new dependency. `components/design-system/FormField.tsx` + `FormSection.tsx` (site-token label/error/slot + grouping fieldset, distinct from the monolith's private `--hos-*` version). `hooks/useRecentValues.ts` (localStorage-backed) + `components/design-system/RecentValueChips.tsx` (presentational; caller owns the single `useRecentValues` instance and passes `values` down — an early version had `RecentValueChips` instantiate its own hook copy, which silently never updated since two hook instances for one storage key don't share state; caught via live Playwright verification, not just visual check, and fixed before shipping). Piloted on `components/patients/AdminPatients.tsx`'s create-patient form, reusing the existing `patientCreateSchema` from `lib/validation/patients.ts` (Track 1.12) for the client-side resolver too — one schema, not two. Grouped into Identity/Contact/Clinical `FormSection`s; "recent chips" wired to the three genuinely-recurring free-text fields (allergies, chronic conditions, current medicines) since they hold comma-separated tokens where a native `<input list>` datalist doesn't fit; the pre-existing duplicate-patient-detection flow (phone-blur lookup, amber banner, same-person/different-person toggle) preserved verbatim by composing RHF's own `onBlur`/`onChange` with the existing side effects. Verified live end-to-end: inline error appears on blur for an empty/invalid phone before any submit attempt, save succeeds with a toast, recent-value chips appear after a save and a chip click correctly appends into the field, duplicate-match banner still fires. **Honestly scoped, not built into this pass**: autosave-on-blur and undo (no natural fit — this is a one-shot create, not an edit-in-place form, and there's no delete endpoint to "undo" against) and a native searchable-combobox (the form's only enum-like fields, gender/blood group, are 3-9 options — forcing a searchable dropdown onto that would be over-engineering). The hook's shape doesn't preclude adding an autosave variant later when a genuine edit-style form adopts it. **Second adoption, 2026-07-12**: `components/inventory/AdminInventory.tsx`'s "Add/update stock item" form. This one also upgraded the underlying schema, not just the form: the server previously validated on a minimal passthrough (`lib/validation/operations.ts`'s original `inventoryCreateSchema`, type-shape only), which silently accepted a blank/negative quantity via `normalizeNumber`'s 0-fallback. New `lib/validation/inventory.ts`'s `inventoryItemCreateSchema` has real rules (name/unit required, category a genuine enum, quantity/reorderLevel coerced numbers `>= 0`) and now powers both the client's inline validation and the server's rejection — confirmed first that no other caller does a partial update through this endpoint (only this form POSTs, only the separate PATCH adjusts quantity), so requiring every field outright is safe. **Found and fixed the same class of bug a third time, this time proactively swept to its source**: `name`/`unit`'s bare `.min(1, msg)` had the identical absent-key-shows-raw-Zod-error bug from the `patient/family` slice — traced it back and fixed 4 more live instances of the exact same pattern in the already-shipped `lib/validation/auth.ts` (`break-glass`, `access/users` create, `approvals`) that were surfaced via `firstZodIssueMessage` and thus actually reaching users; verified all fixed with real requests. Verified live: valid create succeeds, missing name/negative quantity/invalid category/missing quantity all reject with the intended friendly message. **Third adoption, 2026-07-12**: `components/automation/AdminAutomation.tsx`'s "Add manual task" form — a clean 9-field form with no controlled-state entanglement (unlike Lab's create-order form and Communication's message form, both surveyed first and set aside as follow-ups precisely because their visit/recipient/template selects drive other fields' `defaultValue` via a remount-`key` trick, making a faithful RHF migration a larger, riskier change than this slice's scope). New `lib/validation/automation.ts`'s `automationTaskCreateSchema` is a client-only schema (title required, type/priority real enums off `automation-types.ts`, the rest optional) — deliberately layered on top of, not replacing, the route's existing lenient `automationActionSchema` (Track 1.12), since `POST /api/automation` also serves the unrelated "generate" action with a totally different payload shape; the store's own `createAutomationTask` still does the authoritative server-side normalization. Verified live: real login + direct API call with the exact RHF payload shape succeeds and returns the created task; full unit/smoke (254+12) and e2e (19/19) suites pass. **Remaining 11+ forms still on plain FormData** (Lab, Communication, CMS, Finance, HR, and the 3 monolith-embedded forms) — follow-up, module-by-module | Shared RHF+Zod wrapper: smart defaults, autocomplete, recently-used values, searchable dropdowns, grouped sections, inline validation, autosave, undo, success feedback (P4/P6) | Med | L | — |
| 3.3 | 🟡 **Print center** | Investigated on 2026-07-09 via Explore agent before building — confirmed the roadmap's "3 PDFs" claim (`app/api/pdf/{prescription,invoice,discharge-summary}/route.ts`, real `@react-pdf/renderer`/Puppeteer binaries, not print-dialog hacks) and found a real, previously-unknown bug: `DataTable.tsx`'s generic "Print" button (`window.print()`, reused across 10+ admin modules) produced a **blank page on every module** — the global print stylesheet (`app/globals.css`) hides `body *` and only shows `.patient-print`, which no module wrapped its table in. **Fixed**: `DataTable` now renders a hidden `.patient-print`/`.print-data-table` view of the current page's visible rows/columns (dropping `select`/`actions` columns, which aren't meaningful on paper) alongside the interactive table; verified live via `page.emulateMedia({media:"print"})` — headers and rows render correctly, confirmed on the Patients module. Also found the discharge-summary PDF (HTML→Puppeteer path) duplicated its own hex colors/confidentiality text instead of importing them from `lib/pdf/branding.tsx` like the two react-pdf documents do — **unified**: now sources `pdfColors`/`clinicalConfidentialityNote`/`logoPath` from the shared module (pure value substitution, zero behavior change, covered by the existing test suite). **Added one new real document type**: `lib/pdf/medical-certificate-document.tsx` (react-pdf, same architecture as prescription/invoice), built from the same OPD visit fields the prescription PDF already uses (clinicalNote/advice/service) rather than inventing a new certificate-specific data model; new route `app/api/pdf/medical-certificate/route.ts`; download link added next to the existing prescription download in `DoctorPortalWorkspace.tsx`. Verified live: real 52KB PDF, correct `Content-Type`/magic bytes. **Found-in-passing fix**: all 3 pre-existing PDF routes had the same flat-metadata-audit-spread bug as earlier tracks (`metadata: {..., ...auditRequestMetadata(request)}` instead of a typed `device:` field) — fixed across all 4 routes now. **Honestly out of scope this pass** (per user's explicit scoping choice): patient ID cards and barcode/wristband labels — both need a barcode/QR rendering decision (no such library installed; adding one is a deliberate follow-up call, not bundled into this pass) | Unified print surface: + medical certificate, patient card, barcode labels, wristbands; consistent branded templates (P4) | Med | M | — |
| 3.4 | 🟡 **Export center** | Investigated on 2026-07-09 via Explore agent before building. `lib/table-export.ts`'s `downloadCsv` (real, quoted CSV) is genuinely reused by 17 of 23 admin modules through DataTable's `export` prop — a solid foundation. Found: (a) the same file's `downloadExcel` was **dead code, zero call sites**, and didn't even produce real `.xlsx` (an HTML `<table>` served with an Excel MIME type, not OOXML) — asked the user, who chose to delete it rather than invest in a real hand-rolled `.xlsx` writer or a new dependency, given plain CSV already opens natively in Excel; (b) `HospitalOperatingSystem.tsx` (the pre-Track-3.1 monolith) hand-rolled its own duplicate `downloadCsvFile` instead of importing the shared one — converged onto `lib/table-export.ts`'s `downloadCsv` (byte-identical CSV-building algorithm, pure de-dup, zero behavior change — its own `exportExcel`, which IS live/tested via `hospital-os.spec.ts`'s "exports Excel-compatible file" test, was deliberately left untouched since it's a working, tested feature, not the dead stub the user's decision was about); (c) **IPD & Beds was the one DataTable-adopting module with no export at all** — added `export`/`bulkActions` props following the exact same pattern as the other 17 modules. Verified live: CSV downloads with the correct filename/headers/row data, bulk "Export selected" works, and the existing CSV/Excel e2e tests on the monolith's patient-flow table still pass unchanged. **Confirmed correctly out of scope, not gaps**: the other 5 non-DataTable modules (Reports, Readiness, Analytics, Enterprise Modules, Settings) are dashboards/fixed reference data, already evaluated as "not lists" in Track 3.1. **Explicitly deferred, not attempted**: PDF-of-a-table export (thematically Track 3.5's "reports" territory), email delivery (no SMTP exists anywhere in the codebase — only a stub — building real email infrastructure is out of scope for an export feature), and scheduled/encrypted export (the roadmap's own text already defers these to "later") | Central export (PDF/Excel/CSV/print/email; scheduled/encrypted later) reused by every table (P4) | Med | M | 3.1 |
| 3.5 | 🟡 **Reports + analytics drill-down** | Investigated on 2026-07-09 via Explore agent before building — confirmed Reports/Analytics are real server-side aggregations (`lib/reports.ts`/`lib/analytics.ts`, 10+ domain stores, not mock data) with **zero filtering of any kind** (fully unparameterized snapshots) and hand-rolled CSS-width div bars despite Recharts being an installed, used-elsewhere dependency. Critically: **doctor/department filtering as literally described isn't buildable yet** — no OPD visit or appointment record has a doctor-assignment or department field (verified independently, not just trusting the agent); "department" exists only on HR/staff records. Asked the user, who chose the buildable slice: date-range + drill-down + real charts, deferring doctor/department filtering (needs a data-model addition) and saved/scheduled reports (zero persistence/cron/email infrastructure exists) as documented follow-ups. **Built**: `lib/reports.ts`'s `createAdminReport(range?)` — every "today"-scoped field converted from single-day equality to a `from`/`to` range check, defaulting to today when omitted (byte-for-byte same result for every existing caller); `lib/analytics.ts`'s `createAnalyticsSnapshot(windowDays = 14)` — the hardcoded 14-day trend window is now a caller-supplied 7/14/30/90 (validated via new `lib/validation/reports.ts`), default unchanged so `createHospitalOsDashboardMetrics`/`createHospitalOsTrend`/the Hospital OS snapshot route keep working identically. New `lib/report-drilldown.ts` + `app/api/reports/drilldown/route.ts`: a fixed map from a tile's metric key (e.g. `appointments.new`, `risks.lowStockItems`) to its underlying real records — date-range-scoped for "today" tiles, deliberately NOT range-scoped for risk-watchlist tiles (current-state gauges like "patients flagged right now" have no "between these dates" meaning in the data model, an honest distinction preserved rather than forced). New shared `components/design-system/DrilldownPanel.tsx` — click-to-expand inline table + CSV export (Track 3.4's `downloadCsv`), self-contained so a new drillable tile never requires wiring filters into whatever module normally owns that data. Wired into a **representative subset**, not literally every tile: Reports' "Appointments Today"/"OPD Visits Today"/"Low Stock Items" KPI tiles + the New/Confirmed/Urgent appointment-funnel rows; Analytics' full risk watchlist (7 rows) — the rest (current-state totals like "Patient Records," "Staff Present") stay view-only since they're not naturally drillable to one clean underlying list. Replaced `AdminAnalytics.tsx`'s hand-rolled `BarList` internals with real Recharts (`BarChart`/`LineChart`), same external props so every call site (service/symptom/payment mix, workload) was untouched; the 14-day trend became a real `LineChart`. Verified live: date-range widen/reset, both modules' drill-down panels expand with real rows and a working CSV export, the trend-window selector re-fetches, Recharts SVGs render, full e2e suite (19/19, including a11y) and unit/smoke suites pass unchanged | Date-range/department/doctor filters, saved & scheduled reports; analytics that "answer questions" with drill-down (P3/P4) | Med | L | 3.1 |
| 3.6 | ✅ **Document management** | Investigated on 2026-07-09 via Explore agent before building — **the roadmap's own premise was wrong**: there was no base64 file storage anywhere. Appointment "report attachments" only ever stored the *filename* as a string (`lib/appointment-types.ts`'s `report?: string`) — the real file never left the browser, handed off manually via a WhatsApp deep link; lab "report references" were free-text labels, not files. This was genuinely greenfield, hinging on a real infrastructure decision (where do file bytes live?) that isn't mine to make alone — asked the user, who chose Postgres `bytea` over S3 (no new service/credentials needed) and over skipping the track. **Built**: new `patient_documents` table (`database/schema.sql`) with a real `bytea` column, following the `audit_events` precedent of a genuinely relational table rather than base64-stuffing a JSONB blob (every other domain still goes through the generic `store_documents` JSONB-per-key store; audit is the only other exception, for the same reason). `lib/documents-store.ts`: dual-mode like every other store — direct SQL against the new table when `DATA_SOURCE=database`, base64-in-JSON-file fallback for local dev (dev-only, since the JSONB-bloat concern is a production-scale one). `group_id`/`version` columns give real version history (uploading against an existing `group_id` increments `version`; `listDocuments` returns only the latest per group, `listDocumentVersions` returns the full chain). New `app/api/documents/{route,download/route,versions/route}.ts`: upload (multipart via the Web-standard `request.formData()`, no multer/formidable needed), list, download/preview (`?mode=inline` skips the Content-Disposition attachment, so browsers render images/PDFs natively — no viewer dependency), version history — all gated by the same `patients` RBAC resource used elsewhere, all audited via the existing `recordAuditEvent` (no separate access-log mechanism, satisfying "access-log" honestly). Validation caps uploads at PDF/JPEG/PNG/WEBP, 10MB (`lib/validation/documents.ts`). New `components/hospital-os/PatientDocumentsSection.tsx`, wired into the Patient Drawer (Track 2.1) — the single highest-leverage placement, reachable from every module a patient name appears in. Verified live end-to-end (fresh patient, no leftover test data): empty state, upload, inline-preview link, download returns correct bytes/content-type, uploading a replacement increments the version and both versions appear in history — one locator-scoping mistake in the verification script itself (matched an unrelated off-screen "History" button elsewhere on `/admin`) was caught and fixed before concluding the feature worked, not mistaken for a product bug. Full e2e suite (19/19, a11y included) and unit/smoke suites pass unchanged | Upload/preview/version history/download/access-log/role-permissions (P4/P5) | Low | L | — |

**Track 3 outcome:** every module runs on a real enterprise table that survives
2,000+ records; forms minimize typing; unified print/export; reports that answer questions.

---

## TRACK 4 — Scale & Future
*Architectural, forward-looking. Lower urgency; do as growth demands.*

| # | Item | Current state | Build | Pri | Effort | Depends on |
|---|---|---|---|---|---|---|
| 4.1 | ✅ **Code-split `/admin` + Lighthouse 95** | Done (2026-07-08) — this one genuinely wasn't built (confirmed: no `next/dynamic`/`IntersectionObserver` anywhere near `/admin`, unlike several other "not started" items this session that turned out already done). New `components/hospital-os/LazyModuleSection.tsx` dynamic-imports each of the 23 `AdminX` components and mounts them only once their `<section>` scrolls within `200px` of the viewport — no JS chunk fetch, no data fetch, until then. `app/admin/page.tsx` now renders `<LazyModuleSection id={module.id} />` per role-visible module instead of 23 static imports + eagerly-instantiated JSX. **Two real bugs found and fixed during live verification, not by inspection alone**: (1) the loader-function map was first passed as a prop from `page.tsx` (a Server Component) to `LazyModuleSection` (`"use client"`) — React rejected this outright ("Functions cannot be passed directly to Client Components"); fixed by moving the loader map inside the client component, keyed by the serializable `id` string only. (2) Jump-nav to a far below-fold module (e.g., "Settings") stopped working after the refactor — a big jump crosses many modules' `200px` trigger zones at once, so a cascade of them start loading concurrently and keep resolving from skeleton to real (taller) content for a few seconds, shifting the page out from under a one-time native anchor scroll; the target section's own mount was waiting on that same settling. Fixed by decoupling the two: `AdminModuleNav` now dispatches a custom `admin-module-jump` event on click that a matching `LazyModuleSection` uses to mount **immediately**, independent of viewport position (confirmed live: content appears in ~250-280ms instead of not mounting at all), while the visual scroll position is corrected best-effort via a few delayed re-scrolls as the page settles. Updated the `admin dashboard mounts major HMS workspaces` smoke test to check the new dynamic-import loader map (in `LazyModuleSection.tsx`) instead of static JSX tags in `page.tsx`, since the whole point of this change is that those tags no longer exist. **Lighthouse-verified** against the production build (not dev mode) with a real authenticated session: **desktop preset (the realistic condition — staff use this on workstations, not throttled mobile devices) scored 99/100, LCP 745ms** — comfortably under the 2.5s target. Also ran Lighthouse's default mobile-throttled preset (4x CPU slowdown + slow network) out of caution: LCP 3986ms there, driven almost entirely by "Render Delay" rather than any single long task (`Total Blocking Time` was 0ms) — informative headroom for a future constrained-device pass, not a regression to fix now, since this is an internal desktop-workstation tool | Lazy-mount below-fold modules (IntersectionObserver), dynamic-import OS sections; measure/verify LCP <2.5s (P5) | High | M | 2.2 |
| 4.2 | 🟡 **Offline queue / retry** | Investigated directly (grep across the repo — no service worker, no `navigator.onLine` usage, no IndexedDB, no PWA dependency, zero relevant git history) before building — confirmed "None" was accurate this time. Before implementing, flagged a real clinical-safety question to the user: the roadmap's own phrasing ("queue mutations... safe retry") could mean a consumer-PWA-style silent auto-replay queue, which is risky here — a prescription or vitals write queued while offline and silently resubmitted minutes later, after the patient's state may have changed, is a stale-data risk the clinician never gets to catch. User chose: **preserve input, explicit retry only, never auto-replay**. Built `hooks/useOnlineStatus.ts` (the canonical `useSyncExternalStore` pattern for browser APIs — SSR-safe, no effect-based subscribe-on-mount), a shared `components/design-system/OfflineBanner.tsx` mounted in `StaffChrome.tsx` (all of `/admin`, `/doctor`, `/login`) and the Hospital OS branch of `AppChrome.tsx` — both live clinical-mutation surfaces. Added `notify.retryable(message, onRetry)` to `lib/notify.ts`, following the exact pattern of the existing `notify.undo` helper (Sonner's native inline action button). **Piloted** on `AdminPatients.tsx`'s create-patient form: the `fetch()` call is now wrapped in try/catch — a thrown network error (not an HTTP error status) shows a retryable toast whose Retry button re-invokes RHF's own `submit` handler (re-reading current form state, not a stale captured copy, so an edit made while the toast is showing isn't silently discarded); the form was never cleared on failure in the first place (`reset()` only runs on success), so "preserve input" was already the natural behavior once the network-error branch existed. Verified live end-to-end: banner appears/disappears correctly on `context.setOffline(true/false)`, a failed submit shows the retryable toast with both form fields still populated, and clicking Retry after reconnecting successfully saves the patient. **Second adoption, 2026-07-12**: rolled the same try/catch + `notify.retryable` pattern into Inventory's stock-item form alongside its Track 3.2 RHF+Zod migration — one coherent change covers both tracks, since a form's submit handler is the natural place both patterns live. **Third adoption, 2026-07-12**: same pairing again on Automation's manual-task form alongside its own Track 3.2 migration. **Remaining ~18 mutation call sites** still without the pattern — a documented, low-risk follow-up, same reusable shape each time | Queue mutations on connectivity loss, safe retry, clear warnings, never silently lose data (P5) | Med | L | — |
| 4.3 | 🟡 **Operational AI assistant** | Investigated on 2026-07-09 before building — confirmed 4 distinct existing "AI" surfaces, only one of which is a real LLM call: `lib/ai-summary.ts`'s "AI Clinical Brief" (patient summary) genuinely calls Claude (`@anthropic-ai/sdk`, already installed and wired, `ANTHROPIC_API_KEY`-gated). The other three — AI case-review urgency routing, the patient-portal symptom checker, the prescription-suggestion widget — are 100% keyword/rules-based, not LLM reasoning, despite the "AI" labeling. Given this is an XL item bundling 5 distinct capabilities (discharge summary, referral/certificate generation, medicine-interaction, low-stock suggestions, general context-aware chat), asked the user which to prioritize rather than guessing at scope — chose **AI-drafted discharge summaries**. **Built**: `lib/ai-discharge-summary.ts`'s `generateDischargeSummaryDraft(admissionId)`, following the exact architecture of the existing, working `generatePatientSummary` (same model, same error handling for auth/rate-limit/refusal, same "review aid, not authoritative" system-prompt constraint, own safety note). New `app/api/ai/discharge-summary/route.ts` (authorize `patients`/view, audited). New `components/ipd/AiDischargeSummaryDraft.tsx`, mirroring `DoctorPortalWorkspace.tsx`'s existing `AiPatientSummaryPanel` UX exactly (generate → read-only preview → explicit "Use this draft" click) — the draft never reaches the real `dischargeSummary` field or gets saved automatically; a human always reviews and confirms first, matching the same safety principle established in Track 4.2. Wired into `AdminIpdBeds.tsx`'s discharge-summary textarea (given a `key` tied to the field's value so it correctly remounts and picks up the draft once "Use this draft" saves it — the field is otherwise an uncontrolled `defaultValue`/`onBlur` input like its siblings). **Found and fixed in passing**: the pre-existing `patient-summary` route had the same flat-metadata-audit-spread bug fixed elsewhere this session. Verified live end-to-end (API validation branches, UI button/error wiring) — the actual Claude call path itself can't be exercised in this dev environment (no `ANTHROPIC_API_KEY` configured here, the same pre-existing constraint the shipped "AI Clinical Brief" feature already has, not a gap introduced by this work). **Deliberately left as follow-ups on the same primitive, not attempted this pass**: referral/certificate generation, an LLM-based medicine-interaction check (Track 0.5 already built a curated, explainable rules-based interaction checker — an LLM version would be a second, less-auditable system for the same clinical-safety concern), low-stock AI suggestions, and a general context-aware/conversational assistant | Context-aware assistant (knows current module/patient): discharge summary, referral/certificate generation, medicine-interaction, low-stock, "next actions" (P3) | Med | XL | Claude API (done) |
| 4.4 | ⏸️ **Multi-tenancy groundwork — deferred** | Investigated on 2026-07-09 before building, and deliberately **not built** after discussing with the user. The "branch switcher" (`HospitalOperatingSystem.tsx`'s "Agra Main" popover, `AdminSettings.tsx`'s "Branches & locations" card) is decorative-only chrome — both explicitly say "stubbed for v1... not connected to a backend," added in the same original commit as RBAC/branding/PDFs (`928914d`), not evolved from any real second-location rollout. `lib/site-data.ts` has exactly one hospital, one address, one doctor. Zero `branchId`/`locationId` anywhere in any store/type; RBAC (`lib/access/matrix.ts`) has no location dimension at all. Git history has no commit ever discussing a real second location or tenant-isolation need. **Decision**: building unused `branchId` scaffolding now, for a business need with no current evidence of existing, would be designing for a hypothetical — directly against CLAUDE.md's "don't design for hypothetical future requirements" and "never ship features without measurable value." Left deferred rather than silently skipped or built speculatively; revisit if/when a real second branch is actually being planned | Thread `branchId` through stores behind a default; branch isolation + central analytics without redesign (P5) | Med | XL | 3.1 |
| 4.5 | 🟡 **Observability** | Investigated on 2026-07-09 before building. Confirmed the roadmap's own "Depends: hosting" note is real and current: no Sentry/error-tracking SDK installed, `.env.example`'s `SENTRY_DSN`/`MONITORING_DSN` are unused placeholders, and `lib/production-readiness.ts`'s "monitoring" check only verifies a DSN env var is *set* — it can't verify anything is actually reporting. More significantly: **zero error capture existed of any kind** — no `app/error.tsx`/`global-error.tsx`, no structured server-side logging anywhere, so an unexpected exception (a bug, a DB blip) vanished into Next's generic 500 page with no permanent record at all. Rather than wait on the hosting/Sentry decision to fix that gap, built the zero-dependency, zero-account groundwork now: `lib/error-logger.ts` (a small structured-JSON `console.error` — most hosts, Vercel included, capture and index stdout/stderr, so this is a real searchable record today, and its call shape is the seam to plug a real error-tracking service into later without touching call sites again); wrapped `lib/database.ts`'s `query()` — the single choke point every database-mode store already funnels through — so a DB failure anywhere in the app is now logged once, centrally, without retrofitting 70+ individual API routes; new `app/error.tsx` (friendly, actionable, never exposes the raw error/stack per the frozen contract, logs a client-console entry with the digest Next.js also prints server-side so the two cross-reference) and `app/global-error.tsx` (catches a root-layout-level failure, necessarily its own bare `<html>/<body>` with inline styles since it may need to render even if the CSS pipeline itself is what broke) — both are genuinely global via Next's file-convention, covering every route with zero sweep. Verified live: a deliberately-thrown test error (temporary route, removed after) was caught, showed the friendly boundary with no stack trace visible, and the structured log with matching digest appeared server-side. **Sentry/GlitchTip wiring itself remains deferred**, exactly as the roadmap already noted, pending the hosting decision — that's an external-account/cost commitment for the user to make, not a code change I should make unilaterally | Wire monitoring/error-reporting (self-hosted Sentry/GlitchTip per infra decision), usage metrics, alerts (P5) | Med | M | hosting |
| 4.6 | ✅ **Radiology / Pathology modules** | Investigated on 2026-07-09 before building — confirmed genuinely didn't exist. Flagged a scope mismatch to the user before starting: the roadmap describes a full in-house "order→schedule→acquire→review→approve→report→notify" pipeline, but `lib/site-data.ts` shows a single-doctor Gastroenterology/Hepatology/Endoscopy practice with no radiologist or pathologist on staff anywhere in evidence — imaging/pathology is realistically done externally. User chose **external-referral tracking** over the full in-house pipeline (and over skipping). **Built**: one unified module for both Radiology and Pathology (same lifecycle, distinguished by a `type` field) rather than two near-duplicate ones — `lib/external-referral-types.ts`/`-store.ts`/`-query.ts`, closely mirroring Lab's architecture (`lib/lab-*`) but with `Ordered→Sent→Result Received→Reviewed→Cancelled` status (no in-house "collect sample"/"process" states) and critical-flagging that's always a reviewing doctor's manual judgment call (no threshold auto-detection, since these results are free text with no structured numeric values to evaluate against a rule, unlike Lab's panic-value thresholds). New `app/api/external-referrals/route.ts`, Zod-validated from the start (`lib/validation/external-referrals.ts`) rather than copying Lab's still-unconverged hand-rolled validation. Reuses the existing `lab-orders` RBAC resource (same staff who order lab tests plausibly handle referral tracking) rather than adding a 16th resource and rewiring 12 roles. **Generalized Track 3.6's document store for reuse**: renamed/moved `PatientDocumentsSection` → `components/design-system/EntityDocumentsSection.tsx` with a new `entityType` prop (`documentEntityTypes` extended to include `"external-referral"`) — the returned report attaches via the exact same upload/preview/version-history flow the Patient Drawer already uses, zero new document-handling code. New `components/external-referrals/AdminExternalReferrals.tsx`, DataTable-based from the start (no later Track 3.1 migration needed), registered as `module-external-referrals` in the shared admin-modules registry (nav/page-rendering both derive from it automatically) and lazy-loaded per Track 4.1's pattern. Verified live end-to-end: create referral → mark Sent → record a critical finding → acknowledge → attach the returned report PDF → confirm it displays in the edit panel with working view/download/history/replace, full unit/smoke/e2e suites (including the "mounts major HMS workspaces" smoke test, confirming the EntityDocumentsSection rename didn't break the existing Patient Drawer usage) pass unchanged | Build as modules mirroring Lab workflow (order→schedule→acquire→review→approve→report→notify) (P3/P4) | Low | XL | 3.1 |
| 4.7 | ✅ **Shift handover · task assignment · internal messaging** | Investigated on 2026-07-09 before building — confirmed auto-task generation is real (9 event sources, `lib/automation-store.ts`) but found two concrete bugs, not just missing features: `owner` was free text with no link to a real staff member, and `GET /api/automation` required `system-settings:view` (admin-only) — so tasks nominally owned by "Reception"/"Lab"/"IPD Team" were **invisible to those roles entirely**, only admins could ever see the queue. No shift-roster, handover-log, or staff-messaging infrastructure exists, and there's no evidence yet of multiple named staff per role needing to hand over to each other (one seeded record per role). Asked the user, who chose to fix the concrete assignment bug rather than build all 3 bundled capabilities or skip entirely. **Fixed**: `AutomationTask` gained `ownerStaffId?: string` (a real `lib/hr-store.ts` `StaffMember.id`; discovered along the way that the RBAC login system's `AccessUser` and the HR module's `StaffMember` are two unrelated identity spaces with no link between them — "tasks assigned to whoever is currently logged in" would need bridging that gap, a materially bigger undertaking than scoped here, so this assigns to a *named staff member* for display/informational purposes, not to "my own queue" by session identity). `canViewAutomationTask`/`canEditAutomationTask` (`lib/automation-types.ts`) gate each of the 9 task types by the same resource its module already uses (Payment Reminder→billing, Lab Delivery→lab-orders, IPD Review→beds, etc.), mirroring the exact `canViewNotificationCategory` pattern from Track 2.4. `app/api/automation/route.ts` rewritten: GET/POST/PATCH open to any authenticated staff role (not just admin) with per-task-type visibility/edit checks; also fixed `actorRole: "admin"` hardcoding (same class of bug fixed elsewhere this session) and the flat-metadata-audit-spread pattern. `AdminAutomation.tsx`'s owner column and manual-create form both became real `&lt;select&gt;`s bound to `listStaff()`. Verified live end-to-end with a real "reception" RBAC login (not the legacy admin cookie, which always resolves to super-admin): the automation queue was a 403 before this fix and is now a 200 correctly filtered to reception's actual permission set (sees IPD Review/Appointment Follow-up, correctly excludes Low Stock/Lab Delivery); assigning a manually-created task to a real staff member correctly resolved and stored both `owner` and `ownerStaffId`; a role attempting to touch a task type outside its permissions correctly gets 404 (not 403 — deliberately doesn't confirm the task's existence to a role that can't view it). **Follow-up closed out 2026-07-12**: re-asked given a master-prompt completion review — user confirmed a real, current need for staff messaging + shift handover, specifically **cross-department notes** (Doctor→Lab, Reception→Nursing, etc.), not person-to-person handover between multiple staff sharing one role (still just one seeded record per role, so the identity-linking gap noted above is unchanged and wasn't touched). Built `lib/staff-notes-types.ts`/`-store.ts`, `lib/validation/staff-notes.ts`, `app/api/staff-notes/route.ts`, `components/staff-notes/AdminStaffNotes.tsx` — a `StaffNote` has `category` ("Note" for routine cross-department messages, "Handover" for shift-summary style notes), `fromDepartment`/`toDepartment`, priority, message, optional patient context, and an Open/Acknowledged/Resolved lifecycle. Visibility is department-based, not person-based: each of the 9 departments maps to the existing RBAC resource its role already owns (Doctor→patients, Nursing→beds, Laboratory→lab-orders, Pharmacy→pharmacy-inventory, Billing→billing, Insurance→liaison-notes, HR→hr-records, Administration→system-settings, Reception→appointments) — reusing `canViewNotificationCategory`'s exact `null`-means-visible-to-all convention and Track 4.7's own `canViewAutomationTask` per-category-resource pattern, so no new RBAC resource was added. Since no single existing resource covers every role that needs to see this module in the sidebar (pharmacist and hr in particular have neither `patients` nor `reports`), widened `AdminModuleDef.resource` to accept `null` (one new call site, `app/admin/page.tsx`'s `visibleAdminModules`) — visible to every authenticated staff role, same reasoning as Announcements. Verified live end-to-end: a note posted (as super-admin) targeting Pharmacy was correctly invisible to a real doctor-role login (0 notes returned, confirming department-gated filtering works), and correctly acknowledgeable/resolvable. Full chain green: `tsc`/`eslint` clean, unit/smoke, 19/19 e2e | User-assigned tasks (priority/due/reassign), shift handover (pending/critical/escalations/ack), secure internal notes/mentions (P3/P4) | Low | L | 2.4 |
| 4.8 | 🟡 **Full i18n + next-themes** | Investigated on 2026-07-09 before building. Confirmed the Hindi toggle is a fully hand-rolled mechanism (`LanguageToggle.tsx`: `useState`+`localStorage`, no context/provider; `[data-hi]{display:none}` by default, flipped via a `body.lang-hi` class in `globals.css` — every "translated" string is a duplicated DOM node, not a runtime i18n lookup) with real but thin coverage (3 of 91 components, ~3-5 of 26 pages) and public-site-only (no Hindi anywhere in `/admin`/`/doctor`). Dark mode is equally hand-rolled (a small Zustand store + two `localStorage` `useEffect`s in `StaffChrome.tsx`) and staff-only, no dark mode on the public site. No `next-intl`/`react-i18next`/`i18next`/`next-themes` installed. **Assessed and confirmed with the user**: adopting a real i18n framework would mean touching nearly every JSX text node in the app for a single-city Agra hospital serving two languages, with no stated need for a 3rd language, currency/timezone formatting, or RTL — the roadmap's own "optionally" on the theme migration agrees. Extending the existing hand-rolled mechanisms is more proportionate than a framework migration; user confirmed this reading. **Extended Hindi coverage** (bounded slice, not exhaustive site-wide translation) to two more high-traffic public pages using the exact existing `data-en`/`data-hi lang="hi"` convention, including the `.inline-lang` wrapper pattern for nested inline spans (verified this matters: without it, `display:revert` on a block-styled `&lt;span&gt;` would collapse the label/value stacking on toggle — caught and fixed via live verification, not assumed): `app/contact/page.tsx` (hero, booking-form intro, visit-info block including a generalized `InfoLine` component, "before you visit" tips) and `app/about/page.tsx` (hero + "Care Focus" list). Verified live: toggling to Hindi shows the correct translated text with no layout regression on both pages. **Deliberately not attempted**: the deeper mission/values prose further down the About page, or the other ~20 untranslated public pages, or any staff-surface Hindi — all left as a documented, larger content-translation follow-up rather than mechanically covering every remaining page in one pass. **Deliberately not attempted**: next-themes migration (the existing store already works; SSR flash-of-theme handling and system-preference sync are the library's main value-adds, marginal for a staff-only, already-functional feature) and extending dark mode to the public site (not requested, no clear need identified) | Proper i18n framework (multi-lang/currency/timezone/RTL); optionally reconcile theme onto next-themes (P5/P6) | Low | L | — |
| 4.9 | ~~**Token-system unification**~~ *(audit addendum — DONE)* | ~~Two token vocabularies~~ `--hos-*` OS shell colors now alias `--site-*` role-for-role (primary→brand, success→teal, warning→gold, danger→coral, bg/surface/muted/border/text/muted-text→mist/surface/soft/line/ink/muted); dark mode needs no color values of its own, only `color-scheme: dark` — verified via screenshot in both modes | Remaining: `EmptyState`/`ModuleEmptyState` and the two Button components are still separate (deliberately — different prop contracts for different surfaces); revisit only if that duplication becomes a real maintenance cost | — | — | — |
| 4.10 | 🟡 **Decompose `HospitalOperatingSystem.tsx`** *(audit addendum)* | Started 2026-07-09, incrementally per the roadmap's own instruction — not attempted as one big-bang rewrite, given this file is the most safety-critical surface in the app (patient registration, billing, clinical notes) and this session repeatedly found subtle bugs hiding in exactly this class of refactor. Mapped the file's ~20 top-level components first, then extracted the 3 genuinely self-contained ones with zero/minimal shared-state coupling: `ShortcutsDialog` (props-only, zero shared deps), `AuditTrailPanel` and `AcceptancePanel` (props-only, needed only a type/constant already living in — or moved into — the shared `lib/hospital-os-data.ts`) → `components/hospital-os/{ShortcutsDialog,AuditTrailPanel,AcceptancePanel}.tsx`. Moved `AuditTrailItem`'s type definition from being locally declared inside the monolith to `lib/hospital-os-data.ts` (where its sibling Hospital-OS types already live), so the extracted panel and the still-monolithic forms that produce audit items share one source of truth instead of the monolith reverse-exporting a type to its own extracted child. File went from 2,171 → 2,054 lines. **Found and fixed one genuine pre-existing gap in passing**: a smoke test asserted the literal string "Session audit trail" existed inside `HospitalOperatingSystem.tsx` directly — updated it to check the new `AuditTrailPanel.tsx` location instead of leaving a test that would silently stop verifying anything meaningful. Verified live end-to-end: a real patient registration still correctly produces a visible audit-trail entry in the now-extracted panel (proof the props/state boundary carried over exactly), the Shortcuts dialog still opens via `?` and lists shortcuts, full e2e suite (19/19) and unit/smoke suites pass. **Second slice, 2026-07-12**: extracted `CommandPalette` (props-only, zero shared-state coupling, its one type dependency `CommandRecord` already lived in `lib/hospital-os-data.ts`) and `AssignDoctorDialog` (same profile; its two local dependencies `DoctorAssignment`/`assignableDoctors` moved to `lib/hospital-os-data.ts`, matching the `AuditTrailItem` precedent). The monolith's private `FormField` (`--hos-*` tokens, distinct from the site-token `components/design-system/FormField.tsx`) is still used by several components not yet extracted (`PatientPortalPanel`, `PatientRegistrationForm`, `AppointmentBookingForm`, `BillingForm`), so rather than duplicating it into `AssignDoctorDialog`'s new file, it moved to a new shared `components/hospital-os/HosFormField.tsx` that every remaining call site in the monolith now imports too — benefiting those forms' future extractions. File went 2,054 → 1,919 lines across both extractions. Verified live each time: full unit/smoke suites and 19/19 e2e (including the command-palette keyboard-shortcut test and the "assigns doctor" row-action test that directly exercise the two extracted components) pass unchanged. **Third slice, 2026-07-12**: extracted the doctor/patient clinical cluster — `PatientWorkspace` (patient-facing tabbed summary/timeline/vitals/etc. shell), `DoctorWorkspace` (consultation queue + keyboard navigation), `AiPrescriptionAssistant` (AI suggestion confirm/audit flow), and `ClinicalNotesEditor` (debounced autosave + audit flow) → `components/hospital-os/{PatientWorkspace,DoctorWorkspace,AiPrescriptionAssistant,ClinicalNotesEditor}.tsx`. `statusTone` (the status-badge color map, needed by both the monolith's own remaining `OperationsTable` cell renderers and the extracted `DoctorWorkspace`'s queue badges) moved to `lib/hospital-os-data.ts`, same "shared dependency, one source of truth" pattern as `AuditTrailItem`. File went 1,919 → 1,655 lines. Verified live: `tsc --noEmit` clean, `eslint` clean, unit+smoke (254 + 12) pass, and — after confirming no stale dev server was holding port 3100 from earlier manual testing — full e2e suite (19/19) passes, including the three tests that directly exercise these components ("doctor workspace autosaves clinical notes", "doctor workspace queue supports keyboard navigation", "doctor workspace confirms AI prescription suggestion") plus the axe-checked registration/booking/billing flow. **Fourth slice, 2026-07-12**: extracted `TopNav` (header bar: sidebar/palette/shortcuts triggers, live clock, branch-switcher popover, notifications, theme toggle) and `DashboardOverview` (KPI metric cards + revenue/OPD trend charts + realtime activity feed) → `components/hospital-os/{TopNav,DashboardOverview}.tsx`. `metricIcons` (label → Lucide icon map) and the `HospitalTrendPoint`/`RealtimeMessage` types moved to `lib/hospital-os-data.ts`, same shared-dependency pattern as `statusTone`/`AuditTrailItem`. In the same pass, cleaned up `LiveClockWeather`, `NotificationCenter`, `PatientClinicalSnapshot`, and `PatientTimelinePanel` imports that the *previous* (third) slice had left dead in the monolith after extracting `PatientWorkspace` — plus a dozen now-unused UI/icon imports (`Avatar`, `Popover*`, `MetricCard`, `Menu`, `Sun`, `Moon`, `Command`, `MessageSquare`, `Plus`, `QrCode`, `Check`, `CircleDollarSign`, `Bar`) this slice itself orphaned. Found by diffing each ambiguous symbol's occurrence count against the file before/after, since neither `tsc` (no `noUnusedLocals`) nor this repo's `eslint-config-next` flags unused imports — a real blind spot worth remembering for future slices; every extraction from here on should get this same manual sweep, not just a clean `tsc`/`eslint` pass. File went 1,655 → 1,451 lines. Verified live: `tsc --noEmit` and `eslint` clean, unit+smoke (254 + 12) pass, full e2e (19/19) passes including the command-palette and keyboard-shortcuts tests that exercise `TopNav`'s trigger wiring and the axe-checked dashboard test that exercises `DashboardOverview`, plus a manual dev-server fetch of `/mudgalgastromedics-os` confirming a clean 200 with no server errors. **Fifth slice, 2026-07-12**: extracted `PatientPortalPanel` (patient-facing preview: portal summary cards, vitals-trend chart, AI symptom checker, teleconsultation-request dialog) → `components/hospital-os/PatientPortalPanel.tsx` — fully self-contained, all state local to the component, only prop is `onAuditEvent`. In the same pass, ran a systematic dead-import sweep (a small Python script diffing every imported identifier's usage count against the rest of the file, since neither `tsc` nor this repo's `eslint-config-next` catches unused imports) and found **14 more imports already dead from earlier slices** that a plain `tsc`/`eslint` pass had missed: `ClipboardList`, `HospitalRole` (type), `Label`, `LineChart`, `Separator`, `Sparkles`, `Stethoscope`, `Tabs`/`TabsContent`/`TabsList`/`TabsTrigger`, `hospitalRoles`, `patientFlowRows`, `v1AiScope` — plus `autosaveClinicalNotes`/`confirmAiPrescriptionSuggestion` (dead since the third slice) and `requestTeleconsultation` (dead from this slice), all removed. Also fixed a stale smoke-test assertion (`scripts/smoke-tests.mjs`) that still checked the monolith directly for the literal string "AI symptom checker" — same class of gap as the first slice's "Session audit trail" fix, now pointing at `PatientPortalPanel.tsx`. File went 1,451 → 1,263 lines. **This dead-import sweep is now a standing step for every future slice**, not just this one — a clean `tsc`/`eslint` pass alone does not mean no cleanup is needed. Verified live: `tsc --noEmit` and `eslint` clean, unit+smoke (254 + 12) pass, full e2e (19/19) passes including "patient portal symptom checker stays informational" which directly exercises the extracted component. **The remaining ~8 components are a documented follow-up** (`PatientRegistrationForm`, `AppointmentBookingForm`, `BillingForm`, `OperationsTable` — the last being the biggest and highest e2e-risk at ~200 lines, backing nearly all of `hospital-os.spec.ts`'s patient-flow-table tests), extracted opportunistically whenever each is next touched, per the roadmap's own "do incrementally" instruction — not swept in one pass, matching the deliberately small, low-risk scope established since the first slice | Behavior-preserving extraction into `components/hospital-os/` feature modules; do incrementally whenever the file is touched (P7 "no god components") | Med | M–L | pairs well with 1.7 |
| 4.11 | ✅ **Announcements · widgets deferred** *(audit addendum)* | Investigated on 2026-07-09 before building — confirmed both halves were genuinely absent. Asked the user which to prioritize; chose **build announcements, defer movable/resizable widgets**. **Built as a natural extension of Track 2.4's notification inbox, not new infrastructure**: added `"Announcement"` to `lib/notification-types.ts`'s `NotificationCategory` union and `categoryResource` map with `null` (visible to every authenticated staff role, the same treatment as `Emergency`) — so an announcement is just a `StaffNotification` created via the existing `createNotification()`, with no new GET endpoint needed (it surfaces automatically through `/api/notifications`, grouped under its own category header in `NotificationCenter.tsx` alongside everything else). Confirmed the chosen `announcement:` source prefix doesn't collide with `lib/notification-rules.ts`'s `notificationRulePrefixes`, so `syncNotificationsFromOperations` never tries to auto-resolve one the way it does rule-derived alerts (an announcement has no "clearing condition"). New `app/api/announcements/route.ts` — POST-only, gated `authorize(request, "system-settings", "edit")`, Zod-validated (`lib/validation/announcements.ts`: title ≤120 chars, message ≤600 chars, priority enum), audited with the correct `device:` metadata shape from the start. New compose UI in `components/hospital-os/NotificationCenter.tsx` (an "Announce" toggle button + inline title/message/priority form + "Post to all staff"), shown unconditionally rather than role-gated client-side — per CLAUDE.md's "UI visibility is never the only gate," the server is the actual enforcement point, and a non-permitted role gets a clear error toast, not a hidden button masquerading as security. **Verified live end-to-end via authenticated API calls against the running dev server** (no interactive browser tool was available this session, so this substituted for Playwright's manual click-through; the full e2e/a11y suite below covers the UI layer): logged in as the legacy admin cookie (resolves to `super-admin`), posted an announcement, confirmed it appeared via `GET /api/notifications`; logged in separately as the doctor passcode (`main-doctor` role, which has no `system-settings` permission at all) and confirmed that role **can see** the same announcement (proving the visible-to-all read design) but **cannot post one** — a POST attempt correctly returned 403 "Your role does not permit this action." Also surfaced, then fixed on user request: the RBAC matrix's named `admin` role had only `view` on `system-settings` (not `edit`), so only `super-admin` could post an announcement despite the design intent being "any admin-permission role." Widened `admin`'s `system-settings` grant to `["view", "edit"]` in `lib/access/matrix.ts` — checked the blast radius first (the only other `system-settings:edit` gate in the app is `app/api/hms/route.ts`'s internal HMS build-tracker record create/update, a screen `admin` already has `view` access to, so this is a proportionate widening, not a new surface exposed). Verified live end-to-end with a **real `admin`-role RBAC session** (not the legacy super-admin cookie): created a throwaway user, completed the full forced password-change + TOTP MFA enrollment flow (`admin` is an MFA-mandatory role), confirmed the resulting session shows `system-settings: ["view", "edit"]`, and confirmed it can now POST `/api/announcements` (200, not 403) and see the result via `GET /api/notifications` — cleaned up (announcement archived, throwaway user suspended) after. Also ran `scripts/verify-auth-flows.mjs` (22/22) per CLAUDE.md's "after anything near auth/RBAC" requirement. Full verification chain green: `tsc`/`eslint` clean, 254/254 unit + 12/12 smoke, 19/19 e2e including the a11y suite, 22/22 auth-flow checks. **Widgets deliberately deferred**: movable/resizable dashboard widgets have no existing layout-persistence primitive to build on and depend on Track 2.3's role dashboards; left undone rather than guessed at, per the user's own scope choice | Product decision: build or consciously defer. Widgets: after 2.3 role dashboards. Announcements: could ride on the CMS + notification inbox (2.4) | Low | — | 2.3, 2.4 |
| 4.12 | ✅ **Role-dashboard readiness sweep** *(audit addendum)* | 2026-07-12: an Explore-agent audit scored all 9 role dashboards (Patient/Doctor/Administration/Reception/Billing/Pharmacy/Laboratory/Nursing/Management) against the master prompt; user said **"must complete all"**. Scoped via AskUserQuestion first (Nursing MAR: build; Pharmacy PO: build a basic version, reversing an earlier session's defer; Lab machine/QC: skip; Patient satisfaction: build). **Fixed real bugs found during the audit**: Reception's `beds` permission was view-only despite the IPD module's own "Admit patient" button already being visible to them (`lib/access/matrix.ts` → `readWrite`); Doctor Portal had zero self-scoped view into recent actions (new `app/api/doctor/activity/route.ts` + `components/DoctorRecentActivity.tsx`, deliberately not the super-admin-only hospital-wide audit log). **Real, not fabricated, metrics**: `OpdVisit` gained `doctorName` (set once, automatically, by whichever doctor first writes a clinical field) and `consultationStartedAt` (set once, automatically, on first move to "In Consultation") — `lib/analytics.ts`'s new `doctorProductivity`/`avgWaitMinutes` read only these, starting at "Unassigned"/`null` rather than a guessed baseline; user separately confirmed building Doctor Productivity despite today's single-doctor practice ("we will add more doctor soon"). Lab's `stats.processing` used to lump `"Processing"` and `"Sample Collected"` into one number with no visibility into `"Ordered"` (pending) samples — split into `pendingSamples`/`collectedSamples`/`processing` (`app/api/lab/route.ts`, `components/lab/AdminLab.tsx`). Billing Desk was read-only reporting with no way to actually record a payment or handle a refund from the screen billing staff actually live on — added a Today's Revenue tile, additive `refundStatus`/`refundReason`/`refundAmount`/`refund*By` fields on `OpdVisit` (a `Requested`→`Refunded` flow, audited as `opd.refund.requested`/`opd.refund.completed`, distinct from a routine billing edit), and a Manage panel to record a payment method or start/complete a refund without leaving the Billing module. Doctor Portal gained real "favourites": a new optional `diagnosis` field (short structured impression, separate from the free-text clinical note) plus the existing `prescription` field are frequency-counted per doctor (`topFrequent()` — a value must repeat **at least twice** before it's offered as a favourite, so a single one-off note is never surfaced as a habit) and shown as one-click quick-insert chips, only while the field is still empty so they never overwrite something already typed. Doctor Portal also gained a **New Consultation** walk-in action (`app/api/opd` POST now accepts either an existing `appointmentId` **or** a `patientName`/`phone`/`service` triple — fully additive, the original shape is untouched — creating an internal `Appointment` record without sending the public "request received" emails, since the patient is already in front of the doctor); `duty-doctor`'s `appointments` permission widened to include `create` so this is usable by both doctor roles, not just Main Doctor. **New, real "basic" features per explicit user request**: Pharmacy Purchase Orders (`lib/purchase-order-{types,store}.ts`, `app/api/purchase-orders/route.ts`, `components/inventory/AdminPurchaseOrders.tsx`, Zod-validated from the start) — low-stock items are grouped by vendor into one-click draft orders, and marking an order **Received** is the one status transition with a real side effect: it actually restocks `lib/inventory-store.ts` quantities, not a fabricated counter. Nursing MAR (`MedicationOrder`/`MedicationAdministration` on the existing IPD admission record, `components/ipd/AdminMedicationRecord.tsx`) splits prescribing (`prescriptions:edit` — doctors order what to give) from administering (`beds:edit` — nurses record each dose Given/Missed/Refused against that order), matching the same doctor/nurse authority split already used elsewhere in IPD. **Deliberately skipped, not silently dropped**: Lab machine/QC tracking (calibration logs, instrument status, quality-control runs) — this hospital runs lab testing without in-house analyzer/QC infrastructure to track (same single-doctor-practice reasoning as Track 4.6's radiology/pathology scope call), so building the screen would mean fabricating data with nothing real behind it; the user agreed to skip it rather than build a hollow module. **Patient satisfaction feedback** (closed out same day): new `lib/feedback-{types,store}.ts` — a patient can rate a completed OPD visit 1–5 stars with an optional comment, one submission per visit (enforced server-side, not client-side), reachable only for the patient who owns that visit's phone number. `app/api/patient/feedback/route.ts` (patient-session-gated POST) and `app/api/feedback/route.ts` (admin-facing GET, gated on the existing `reports` resource — no new RBAC surface needed). `lib/analytics.ts`'s new `patientSatisfaction` (`averageRating`/`totalResponses`/star `distribution`) reads only real submitted ratings — `averageRating` is `null`, not `0`, until at least one exists, same discipline as `avgWaitMinutes`. New `components/PatientFeedbackWidget.tsx` prompts for a rating on the patient's most recent completed visit inside the Patient Portal; `AdminAnalytics.tsx` gained an executive tile plus a star-distribution BarList card. Verified live end-to-end: real appointment → OPD visit → marked Completed → patient OTP login → submitted 5★ feedback (200) → resubmission on the same visit correctly rejected (400, "already submitted") → admin `GET /api/feedback` and `GET /api/analytics` both reflect the real rating. **New Dietitian role**: added to `lib/access/matrix.ts` with a narrowly-scoped `diet-plans` RBAC resource (separate from general `patients:edit`) — the role can view patients and bed/admission lists, and write a new persistent `PatientRecord.dietPlan` plus the existing IPD `dietAdvice` field, but nothing else on either record; `main-doctor`/`duty-doctor`/`admin` were granted `diet-plans` too so their existing `dietAdvice`-editing ability wasn't lost when it moved off the `patients` gate. New `components/patients/AdminDietPlans.tsx` (patient search + currently-admitted list, both with inline diet-plan editors), registered as `module-diet-plans` and given its own `lib/role-dashboard.ts` band ("Nutrition today"). Verified live end-to-end with a real throwaway Dietitian login: correct 200s on patients/beds view and diet-plan writes, correct 403s on admitting a patient, editing a patient's name, and viewing HR. **Purchase Orders "premium" follow-up** (same day, user-requested on top of the basic version): a branded PDF per order (`lib/pdf/purchase-order-document.tsx`, mirrors the existing invoice/prescription PDF pattern exactly), a status filter plus CSV export for order history, and an optional `expectedDeliveryDate` with an overdue banner once an `Ordered` order passes that date without being `Received`. Verified live: created a real order, marked it Ordered with a past delivery date, confirmed the overdue flag fired and the PDF downloaded as real, valid bytes (200, `application/pdf`, starts with `%PDF-`). Full verification chain across every item in this track: `tsc`/`eslint` clean, 254/254 unit + 12/12 smoke green, and `scripts/verify-auth-flows.mjs` (22/22) re-run given the scale of RBAC changes | Close every gap the 2026-07-12 role-dashboard audit found, module by module, per explicit user instruction | High | L | 2.3 |

---

## Recommended execution order

*Reordered to honor the Part 8 decision hierarchy (Patient Safety → … → Visual Design last).*

1. **Track 0: 0.1 (allergy alert) + 0.3 (duplicate-patient prompt) + 0.4 (duplicate meds)** —
   highest-value safety wins, mostly S/M effort; do these first regardless of visual polish.
2. **Track 1 in full** (about a week) — biggest visible lift for least risk; makes the
   platform *feel* like one enterprise product. Runs alongside the rest of Track 0.
3. **Track 2: 2.4 (notifications) → 0.2 (critical-lab flagging, which depends on it) →
   2.1 (Patient Drawer) + 2.2 (role-filtered modules)** — safety + the items that most
   change day-to-day work.
4. **Track 3: 3.1 (DataTable)** — the single largest lever for real-hospital data volume;
   independent, can run in parallel with late Track 2.
5. **Track 0: 0.5–0.7 (interactions, identity, consent) + Track 4** — pull in as data
   sources, clinical scope, and growth (multi-branch, observability) demand.

## Cross-cutting discipline (apply to every item)
- Reuse-first: search the design system before creating a component.
- Keep it backward-compatible; never weaken auth/RBAC; every mutation stays audited.
- Verify each change end-to-end (tsc/lint/build/unit/smoke/e2e + a live check) before
  calling it done — the standing practice this project already follows.
- Re-run `BASE=<url> node scripts/verify-auth-flows.mjs` after anything near the auth path.

## What is explicitly already DONE (not on this roadmap)
Auth + RBAC (backend-enforced), audit logging, the Postgres document-store migration,
strict typing (zero `any`), security headers, adapter-based SMS/email/rate-limit seams,
branding + website↔OS separation, the workspace launcher, dark mode across staff
surfaces, skeleton loaders, the OS route rename + sidebar navigation, density pass,
Hindi booking/portal flows, live IST clock + weather, and the PDF generation feature.
